export const metadata = {
  title: 'L06 · Γραφήματα Ι — Βασικές Έννοιες',
  description:
    'Η εισαγωγή στο μεγαλύτερο κομμάτι του μαθήματος: τι είναι γράφημα, πώς το αποθηκεύουμε (πίνακας vs λίστα γειτνίασης), διαδρομές, κύκλοι, δένδρα, και τα προβλήματα s-t συνεκτικότητας και απόστασης που θα λύσουν τα L07–L09.',
}

export const frontmatter = {
  title: 'L06 · Γραφήματα Ι — Βασικές Έννοιες',
  slug: 'lectures/L06-graphs-i',
  order: 6,
  prerequisites: ['lectures/L01-eisagogika'],
  examWeight: 5,
  estimatedReadTime: 38,
  lastUpdated: '2026-05-23',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Graphs</span>
  <span>·</span>
  <span>Διάλεξη 6</span>
  <span>·</span>
  <span>~38 min</span>
</div>

# L06 · Γραφήματα Ι — Βασικές Έννοιες

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="lectures/L06-graphs-i" />
  <Bookmark id="lectures/L06-graphs-i" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc
  sources={{ pdf: 'L06 - Αλγόριθμοι σε Γραφήματα Ι.pdf' }}
  note="33 διαφάνειες — το θεμελιωτικό κεφάλαιο των γραφημάτων· ορολογία και διατύπωση προβλημάτων, χωρίς αλγόριθμο ακόμα."
/>

## Τι θα δούμε

Με αυτή τη διάλεξη ξεκινά το **μεγαλύτερο** κομμάτι του μαθήματος. Σύμφωνα με τα παλιά θέματα, οι αλγόριθμοι σε γραφήματα είναι περίπου το **30–35%** του βαθμού στις εξετάσεις — το μεγαλύτερο μονοκόμματο μπλοκ. Τα τέσσερα επόμενα κεφάλαια ([L07](/lectures/L07-graphs-ii)–[L09](/lectures/L09-graphs-iv)) χτίζονται πάνω σε αυτό εδώ.

Το L06 είναι το **προετοιμαστικό** κεφάλαιο — και θα είναι ειλικρινές μαζί σου: **δεν** θα δεις αλγόριθμο εδώ. Θα δεις τρία πράγματα, και θα τα δεις καλά:

1. **Τι είναι** ένα γράφημα — η ορολογία που θα χρησιμοποιείς σε κάθε επόμενη διάλεξη.
2. **Πώς το αποθηκεύεις** στον υπολογιστή — και γιατί αυτή η επιλογή καθορίζει τις πολυπλοκότητες *όλων* των αλγορίθμων που θα μάθεις.
3. **Τι θα το ρωτήσεις** — τα προβλήματα που θα λύσουν τα L07–L09.

<Callout type="intuition">
**Γιατί αξίζει τόσο χώρο.** Ένα γράφημα είναι ο πιο γενικός τρόπος να εκφράσεις **σχέσεις** ανάμεσα σε αντικείμενα. Όπου υπάρχει ένα «αυτό συνδέεται με εκείνο», υπάρχει γράφημα: κοινωνικά δίκτυα, χάρτες, η ροή ενός προγράμματος, εξαρτήσεις εργασιών. Επειδή η έννοια είναι τόσο γενική, οι αλγόριθμοι που θα μάθουμε εφαρμόζονται **παντού** — γι' αυτό και πέφτουν τόσο συχνά στις εξετάσεις.
</Callout>

## Τι είναι ένα γράφημα

Ένα **μη κατευθυνόμενο γράφημα** (ή απλό γράφημα) γράφεται

$$G = (V, E)$$

και αποτελείται από δύο σύνολα:

- $V$ — το σύνολο των **κορυφών** (vertices, nodes, κόμβοι): τα «αντικείμενα».
- $E$ — το σύνολο των **ακμών** (edges): οι συνδέσεις. Κάθε ακμή είναι ένα **μη διατεταγμένο** ζευγάρι κορυφών $\{u, v\}$ — «μη διατεταγμένο» σημαίνει ότι $\{u, v\}$ και $\{v, u\}$ είναι η ίδια ακμή.

Δύο μεγέθη μετράμε διαρκώς:

- $n(G) = |V|$ — το πλήθος των κορυφών.
- $m(G) = |E|$ — το πλήθος των ακμών.

Και μία έννοια ανά κορυφή: ο **βαθμός** (degree) μιας κορυφής $v$ είναι το πλήθος των ακμών που την ακουμπούν — διαισθητικά, «πόσους γείτονες έχει».

### Ένα παράδειγμα

<div className="graph-canvas">
<svg viewBox="0 0 600 430" className="w-full max-w-xl" xmlns="http://www.w3.org/2000/svg">
  {[[1,2],[1,3],[2,3],[2,4],[2,5],[3,5],[3,7],[3,8],[4,5],[5,6],[7,8]].map(([a,b],i) => {
    const pos = {1:[235,64],2:[130,192],3:[332,186],4:[112,322],5:[286,322],6:[286,388],7:[470,72],8:[476,252]}
    return <line key={i} x1={pos[a][0]} y1={pos[a][1]} x2={pos[b][0]} y2={pos[b][1]} stroke="#9b8a8d" strokeWidth="2" />
  })}
  {[[1,235,64],[2,130,192],[3,332,186],[4,112,322],[5,286,322],[6,286,388],[7,470,72],[8,476,252]].map(([id,x,y]) => (
    <g key={id}>
      <circle cx={x} cy={y} r="23" fill="#ffffff" stroke="#9f1239" strokeWidth="2.5" />
      <text x={x} y={y} textAnchor="middle" dominantBaseline="central" fontSize="15" fontWeight="700" fill="#1c1214">{id}</text>
    </g>
  ))}
</svg>
</div>

Σε αυτό το γράφημα:

- $V = \{1, 2, 3, 4, 5, 6, 7, 8\}$, άρα $n(G) = 8$.
- $E = \{\{1,2\}, \{1,3\}, \{2,3\}, \{2,4\}, \{2,5\}, \{3,5\}, \{3,7\}, \{3,8\}, \{4,5\}, \{5,6\}, \{7,8\}\}$, άρα $m(G) = 11$.
- Οι βαθμοί: $\deg(1) = 2$, $\deg(2) = 4$, $\deg(3) = 5$, $\deg(6) = 1$ (η κορυφή 6 ακουμπάει μόνο μία ακμή).

### Πόσοι βαθμοί συνολικά;

Φαντάσου ότι σε μια συνάντηση κάθε άνθρωπος μετράει πόσα χέρια έσφιξε. Αν αθροίσεις αυτούς τους αριθμούς, παίρνεις **διπλάσιο** του συνολικού πλήθους χειραψιών — γιατί κάθε χειραψία προσμετρήθηκε *δύο φορές*, μία από κάθε άνθρωπο που συμμετείχε. Σε ένα γράφημα ισχύει ακριβώς το ίδιο, με τις ακμές στη θέση των χειραψιών:

<Callout type="key">
**Λήμμα της χειραψίας (handshaking).** Το άθροισμα των βαθμών όλων των κορυφών ισούται με **δύο φορές** το πλήθος των ακμών:

$$\sum_{v \in V} \deg(v) = 2\,m$$

**Γιατί:** κάθε ακμή έχει δύο άκρα, οπότε προσμετράται στον βαθμό **δύο** κορυφών — μία φορά για καθεμία. Άθροισε τους βαθμούς και μετράς κάθε ακμή ακριβώς δύο φορές. Είναι ένα μικρό αλλά πολύ χρήσιμο εργαλείο σε αναλύσεις πολυπλοκότητας.
</Callout>

Δοκίμασέ το ζωντανά — πρόσθεσε ακμές μία-μία και παρακολούθησε τους μετρητές: σε κάθε νέα ακμή, **δύο** βαθμοί τικάρουν +1 και ο μετρητής `2m` τικάρει +2. Δεν θα μπορούσαν ποτέ να βγουν διαφορετικοί:

<HandshakeLemmaViz />

## Πού κρύβονται γραφήματα

Η δύναμη της έννοιας φαίνεται στο πόσο διαφορετικά πράγματα μοντελοποιεί:

| Γράφημα | Κορυφές | Ακμές |
|---|---|---|
| Δίκτυα μεταφορών | διασταυρώσεις | δρόμοι |
| Δίκτυα επικοινωνιών | υπολογιστές | καλώδια |
| Παγκόσμιος ιστός | ιστοσελίδες | υπερσύνδεσμοι |
| Κοινωνικά δίκτυα | άνθρωποι | σχέσεις/φιλίες |
| Τροφική αλυσίδα | είδη | θηρευτής → θήραμα |
| Λογισμικό | συναρτήσεις | κλήσεις |
| Χρονοπρογραμματισμός | εργασίες | περιορισμοί προτεραιότητας |
| Μετρό | σταθμοί | διαδοχικές στάσεις |

### Η μοντελοποίηση είναι επιλογή

Διάβασε αυτόν τον πίνακα προσεκτικά. Σε κάθε γραμμή, κάποιος *αποφάσισε* τι θα είναι κορυφή και τι ακμή. Αλλά **η ίδια κατάσταση μπορεί να γίνει διαφορετικό γράφημα ανάλογα με την ερώτηση** που θέλεις να απαντήσεις. Δύο διαφορετικές απαντήσεις θα ζητούσαν δύο διαφορετικά γραφήματα — από τα ίδια ακριβώς δεδομένα.

Ένα παράδειγμα από το μετρό:

- Αν ρωτάς **«πόσοι σταθμοί χωρίζουν την Α από τη Μ;»** → κορυφές = σταθμοί, ακμές = διαδοχικοί σταθμοί στην ίδια γραμμή. Η απάντηση είναι το μήκος του μονοπατιού.
- Αν ρωτάς **«πόσες αλλαγές γραμμής χρειάζομαι;»** → οι ίδιοι σταθμοί δεν είναι πια κορυφές. Τώρα **οι γραμμές είναι κορυφές**, και υπάρχει ακμή ανάμεσα σε δύο γραμμές όταν μοιράζονται σταθμό μετεπιβίβασης. Η απάντηση είναι το μήκος μονοπατιού στο νέο γράφημα μείον ένα.

Άλλαξε τις «όψεις» παρακάτω και πάρε το ίδιο ζευγάρι αφετηρίας-προορισμού — οι δύο αριθμοί ΔΕΝ ταυτίζονται:

<MetroModelingViz />

<Callout type="key">
**Το πιο σημαντικό μάθημα του L06 για τις εξετάσεις.** Όταν διαβάζεις ένα πρόβλημα σε φυσική γλώσσα — έργα με προτεραιότητες, αρχεία με εξαρτήσεις, σχέσεις γνωριμίας — η δουλειά σου είναι ΠΡΩΤΑ να **διαλέξεις** τι θα είναι κορυφές και τι ακμές, ανάλογα με την ερώτηση. Μόλις η μετάφραση γίνει σωστά, οι αλγόριθμοι των L07–L09 το λύνουν έτοιμοι.
</Callout>

<ExamProblem
  relatedExerciseId="front-set-7-ask2"
  pattern={
    <>
      <strong>Αν δεις γρίφο, παιχνίδι ή σύστημα με «καταστάσεις + κανόνες μετάβασης»</strong> — βαρκάρης που μεταφέρει αντικείμενα απέναντι, κιουμπικός Rubik, ταμπλό όπου μετακινείς πιόνια με συγκεκριμένους κανόνες — η ίδια απόφαση που μόλις διάβασες ξαναπαίζει στην πιο καθαρή της εκδοχή: <em>κόμβοι = ασφαλείς καταστάσεις</em> (αποκλείεις όσες παραβιάζουν κανόνα — π.χ. λύκος μόνος με κατσίκα)· <em>ακμές = επιτρεπτές μεταβάσεις</em> (μία νόμιμη κίνηση πάει από τη μία στην άλλη). Η ερώτηση «μπορώ να φτάσω από αρχή σε στόχο;» γίνεται s-t συνεκτικότητα· αν θέλεις <em>συντομότερη</em> λύση → BFS. Το κέρδος: πληρώνεις <InlineMath>{'O(|V_{\\text{κατ}}| + |E_{\\text{κατ}}|)'}</InlineMath> στο μέγεθος του <em>γράφου καταστάσεων</em>, όχι στον αρχικό χώρο αναζήτησης — και ο χώρος μικραίνει δραστικά μόλις αποκλείσεις τα μη-ασφαλή (στο πρόβλημα: 10 ασφαλείς από 16 δυνητικές). <em>Σήμα στην εκφώνηση</em>: «κανόνες», «επιτρέπεται», «δεν επιτρέπεται», «μετακίνηση» — οι λέξεις της μετάβασης.
    </>
  }
/>

## Πώς αποθηκεύουμε ένα γράφημα

Πριν τρέξουμε οποιονδήποτε αλγόριθμο, πρέπει να βάλουμε το γράφημα στη μνήμη. Υπάρχουν **δύο** βασικοί τρόποι — και η επιλογή δεν είναι λεπτομέρεια: καθορίζει την πολυπλοκότητα **κάθε** αλγορίθμου που θα γράψεις από εδώ και πέρα.

### Πίνακας γειτνίασης (adjacency matrix)

Ένας τετραγωνικός πίνακας $A$ διαστάσεων $n \times n$, όπου το κελί $A_{ij}$ λέει αν υπάρχει ακμή ανάμεσα στις κορυφές $i$ και $j$:

$$A_{ij} = \begin{cases} 1 & \text{αν } \{i, j\} \in E \\ 0 & \text{διαφορετικά} \end{cases}$$

Σε μη κατευθυνόμενο γράφημα η ακμή $\{i,j\}$ είναι ίδια με την $\{j,i\}$, οπότε ο πίνακας είναι **συμμετρικός**: $A_{ij} = A_{ji}$.

### Λίστα γειτνίασης (adjacency list)

Αντί για έναν μεγάλο πίνακα, κρατάμε $n$ μικρές λίστες — μία ανά κορυφή — όπου η λίστα της $v$ περιέχει **ακριβώς τους γείτονες** της $v$.

### Δες τες δίπλα-δίπλα

Το σχήμα, ο πίνακας και η λίστα είναι **η ίδια πληροφορία** ντυμένη αλλιώς. Διάλεξε μια κορυφή στο εργαλείο και πρόσεξε πώς ανάβει ταυτόχρονα και στις τρεις όψεις — και τι κοστίζει να βρεις τους γείτονές της σε καθεμία:

<GraphRepresentations />

### Ποια να διαλέξω;

Οι δύο αναπαραστάσεις δεν είναι ισοδύναμες — η καθεμία είναι καλή σε άλλο πράγμα:

| | Πίνακας γειτνίασης | Λίστα γειτνίασης |
|---|---|---|
| Χώρος | $\Theta(n^2)$ | $\Theta(n + m)$ |
| «Υπάρχει η ακμή $\{i,j\}$;» | $\Theta(1)$ ✓ | $O(\deg(i))$ |
| Σάρωση γειτόνων της $v$ | $\Theta(n)$ | $\Theta(\deg(v))$ ✓ |
| Σάρωση όλου του γραφήματος | $\Theta(n^2)$ | $\Theta(n + m)$ ✓ |

<Callout type="key">
**Ο κανόνας.**

- **Πυκνό γράφημα** ($m$ κοντά στο $n^2$ — π.χ. πλήρες γράφημα): **πίνακας γειτνίασης**. Ο χώρος $n^2$ δεν σπαταλιέται, και κερδίζεις το $O(1)$ edge lookup.
- **Αραιό γράφημα** ($m = O(n)$ ή $O(n \log n)$ — το **σύνηθες** στην πράξη): **λίστα γειτνίασης**. Γραμμικός χώρος, και η σάρωση γειτόνων κοστίζει όσο ο βαθμός, όχι $n$.

**Στο μάθημα**, όταν βλέπεις πολυπλοκότητα γραμμένη με $m$ — π.χ. $O(n + m)$ — υπονοείται **λίστα γειτνίασης**. Σχεδόν όλοι οι αλγόριθμοι γραφημάτων σαρώνουν γείτονες ασταμάτητα, οπότε η λίστα είναι η προεπιλογή.
</Callout>

<RecallCard
  algorithm="Αναπαράσταση γραφήματος"
  keywords={['πίνακας n×n', 'λίστα n+m', 'O(1) έλεγχος ακμής', 'O(deg) σάρωση γειτόνων', 'πυκνό vs αραιό']}
  skeleton={[
    'Κοίτα τον λόγο του m προς το n — πόσο πυκνό είναι το γράφημα;',
    'm κοντά στο n² (πυκνό) → πίνακας γειτνίασης.',
    'm = O(n) ή O(n log n) (αραιό — το σύνηθες) → λίστα γειτνίασης.',
    'Βλέπεις πολυπλοκότητα με m μέσα; Υπονοεί λίστα γειτνίασης.',
  ]}
  complexity="πίνακας Θ(n²) · λίστα Θ(n+m)"
  trap="Ο πίνακας απαντά «υπάρχει η ακμή {i,j};» σε O(1) — αλλά για να βρεις όλους τους γείτονες μιας κορυφής σαρώνει ολόκληρη γραμμή, O(n), ακόμα κι αν ο βαθμός είναι 1. Η λίστα διαβάζει ακριβώς O(deg). Οι αλγόριθμοι γραφημάτων σαρώνουν γείτονες συνεχώς — γι' αυτό σχεδόν πάντα λίστα."
/>

<ExamProblem
  relatedExerciseId="pt6-th1"
  pattern={
    <>
      <strong>Αν η εκφώνηση δίνει αναπαράσταση και ζητάει πολυπλοκότητα BFS/DFS ή εύρεση γειτόνων <InlineMath>{'N(v)'}</InlineMath></strong> — δες ποια λέξη υπάρχει ρητά. <em>Λίστες</em>: BFS/DFS τρέχει <InlineMath>{'O(|V| + |E|)'}</InlineMath>, οπότε αν η εκφώνηση επιπλέον λέει <InlineMath>{'|E| = \\Theta(|V|)'}</InlineMath> (αραιός) η απάντηση καταρρέει σε <InlineMath>{'O(|V|)'}</InlineMath>· εύρεση <InlineMath>{'N(v)'}</InlineMath> κοστίζει ακριβώς <InlineMath>{'O(\\Delta(v))'}</InlineMath>, όσοι γείτονες υπάρχουν. <em>Πίνακας</em>: η ίδια εύρεση κοστίζει <InlineMath>{'O(|V|)'}</InlineMath> ανεξάρτητα από τον πραγματικό βαθμό, και το BFS γίνεται συνολικά <InlineMath>{'O(|V|^2)'}</InlineMath> για τον ίδιο λόγο. Το «αραιό + λίστες» είναι αυτό που σώζει την πολυπλοκότητα — αυτή είναι ολόκληρη η ερώτηση, όχι παρενθετική λεπτομέρεια. <em>Σήμα στην εκφώνηση</em>: όποτε αναφέρεται η αναπαράσταση ρητά, ή υπάρχει σχέση τύπου <InlineMath>{'|E| = \\Theta(|V|)'}</InlineMath>, η εξέταση τεστάρει τον «κανόνα του πυκνού/αραιού» που μόλις διάβασες.
    </>
  }
/>

## Διαδρομές και συνεκτικότητα

Με το γράφημα αποθηκευμένο, χρειαζόμαστε λεξιλόγιο για να μιλήσουμε για **κίνηση** μέσα του.

### Διαδρομή

Φαντάσου ότι περπατάς πάνω στο γράφημα: κάθε σου βήμα είναι μια ακμή. Μια **διαδρομή** (path) είναι ακριβώς αυτή η περιπατητική: μια ακολουθία κορυφών

$$P = v_1, v_2, \dots, v_{k-1}, v_k$$

όπου κάθε διαδοχικό ζευγάρι $\{v_i, v_{i+1}\}$ είναι **ακμή** του γραφήματος — δηλαδή κάθε βήμα είναι «νόμιμο», ακολουθεί υπαρκτή σύνδεση. Το **μήκος** της διαδρομής είναι το πλήθος των βημάτων, $k - 1$.

Μια **απλή διαδρομή** είναι μια διαδρομή όπου όλες οι κορυφές είναι **διαφορετικές** — δεν περνάς δύο φορές από τον ίδιο κόμβο.

**Γιατί μας νοιάζει να είναι απλή;** Σκέψου το αλλιώς: αν μια διαδρομή ξαναπεράσει από κάποιον κόμβο, τότε ανάμεσα στις δύο επισκέψεις έχει κάνει έναν «κύκλο» — μια κυκλική περιπλάνηση που τη φέρνει πίσω στο ίδιο σημείο. Αν σου ενδιαφέρει το *μήκος* (π.χ. ψάχνεις τη **συντομότερη** διαδρομή), αυτός ο κύκλος είναι περιττός: κόψε τον, και βγαίνεις με τα ίδια άκρα αλλά **μικρότερο** μήκος. Γι' αυτό μια συντομότερη διαδρομή είναι **πάντα** απλή.

Δοκίμασέ το: φτιάξε μια διαδρομή κάνοντας κλικ σε κορυφές με τη σειρά. Αν επαναλάβεις σκόπιμα μια κορυφή, το «ψαλίδι» θα κόψει τον κύκλο μπροστά σου, και το μήκος θα πέσει:

<PathBuilder />

### Συνεκτικότητα

Ένα μη κατευθυνόμενο γράφημα είναι **συνεκτικό** (connected) αν για **κάθε** ζευγάρι κορυφών $u, v$ υπάρχει διαδρομή που τις ενώνει. Διαισθητικά: κανείς δεν είναι «νησί» — από όπου κι αν ξεκινήσεις, μπορείς να φτάσεις παντού.

Αν το γράφημα δεν είναι συνεκτικό, σπάει φυσικά σε κομμάτια: τις **συνεκτικές συνιστώσες** (connected components). Κάθε συνιστώσα είναι ένα μέγιστο υποσύνολο κορυφών μέσα στο οποίο **όλοι φτάνουν σε όλους** — αλλά **κανείς δεν φτάνει** σε κάποιον έξω από τη συνιστώσα.

Ο πιο φυσικός τρόπος να δεις τι είναι μια συνιστώσα είναι να ξεκινήσεις από μια κορυφή και να αφήσεις ένα «κύμα» να εξαπλωθεί. Όπου σταματάει το κύμα — εκεί είναι το όριο της συνιστώσας:

<ConnectivityExplorer />

<ExamProblem
  relatedExerciseId="pt5-th1"
  pattern={
    <>
      <strong>Αν δεις «δίνεται μη κατευθυνόμενος γράφος σε λίστες γειτνίασης — βρες τις συνεκτικές συνιστώσες σε βέλτιστη πολυπλοκότητα»</strong> — είναι το «κύμα ανά νησί» που μόλις είδες, γραμμένο ως αλγόριθμος δύο γραμμών. Συνταγή: <em>εξωτερικός βρόχος</em> που σαρώνει τις κορυφές ψάχνοντας την επόμενη ασημάδευτη + <em>ένα BFS</em> (ή DFS) ανά νέα συνιστώσα που σημαδεύει όλο το νησί με κοινό id <InlineMath>{'c'}</InlineMath>. Κάθε κορυφή μπαίνει στην ουρά το πολύ μία φορά → <InlineMath>{'O(|V|)'}</InlineMath>· σε λίστες, κάθε ακμή σαρώνεται ακριβώς δύο φορές (μία από κάθε άκρο) → <InlineMath>{'O(|E|)'}</InlineMath>· σύνολο <InlineMath>{'\\Theta(|V| + |E|)'}</InlineMath>. Αυτό είναι και το <em>θεωρητικό κάτω φράγμα</em> του προβλήματος: ένας αλγόριθμος <em>πρέπει</em> να αγγίξει την είσοδο για να αποφασίσει για αυτήν, οπότε γραμμικός = βέλτιστος. <em>Πρότυπο σκέψης</em>: όποτε ζητείται «σπάσε γράφο σε μέγιστα συνεκτικά κομμάτια» (συνιστώσες, χρωματισμός με ελάχιστα χρώματα, ομαδοποίηση δικτύου), η σκαλωσιά είναι ίδια — «βρες-την-επόμενη-ασημάδευτη + κυρτό BFS/DFS από αυτήν».
    </>
  }
/>

### Κύκλος

Ένας **κύκλος** (cycle) είναι, με λίγα λόγια, μια απλή διαδρομή **που κλείνει** — ξεκινάς από κάπου, περνάς από διαφορετικές κορυφές, και επιστρέφεις στην αφετηρία ακολουθώντας μια ακόμα ακμή. Τυπικά, μια ακολουθία $v_1, v_2, \dots, v_k$ που:

1. **είναι διαδρομή**, και επιπλέον $\{v_1, v_k\} \in E$ — η αρχή ενώνεται με το τέλος με γνήσια ακμή του γραφήματος·
2. **έχει $k \ge 3$** — αυτό αποκλείει τους τετριμμένους «κύκλους» μήκους 2 (πήγαινε-έλα στην ίδια ακμή, που δεν θα μέτραγε ως πραγματικός κύκλος)·
3. **έχει όλες τις κορυφές διαφορετικές** — όπως και στην απλή διαδρομή.

Δοκίμασε να χτίσεις έναν εσύ. Στο εργαλείο παρακάτω, κλικ σε κορυφές για να βαδίσεις· επιστροφή στην αρχή «κλείνει» τη διαδρομή. Οι τέσσερις όροι (κάθε βήμα είναι ακμή, $k \ge 3$, διακριτές, υπάρχει η κλείστρα) ελέγχονται **ζωντανά** — δες ποιον σπας με τα τρία προεπιλεγμένα παραδείγματα:

<CycleExplorer />

## Δένδρα

Ανάμεσα σε όλα τα γραφήματα, μία οικογένεια είναι τόσο σημαντική που έχει δικό της όνομα. Ένα γράφημα είναι **δένδρο** (tree) αν είναι ταυτόχρονα:

1. **συνεκτικό** — όλοι φτάνουν σε όλους, και
2. **ακυκλικό** — δεν περιέχει κανέναν κύκλο.

Διαισθητικά, ένα δένδρο είναι το πιο **οικονομικό** γράφημα που μπορείς να φτιάξεις διατηρώντας τα πάντα συνδεδεμένα: αρκετές ακμές για να φτάνει κανείς οπουδήποτε, αλλά **ούτε μία παραπάνω** που να φτιάχνει κυκλώματα. Όπως ο σκελετός που κρατάει ένα δίκτυο όρθιο χωρίς πλεονασμό.

Αυτή η οικονομικότητα μεταφράζεται σε έναν ακριβή αριθμό — και σε ένα από τα πιο όμορφα θεωρήματα του κεφαλαίου:

<Callout type="key">
**Θεώρημα — οι τρεις ιδιότητες του δένδρου.** Έστω $G$ γράφημα με $n$ κορυφές. Από τις τρεις παρακάτω προτάσεις, **οποιεσδήποτε δύο συνεπάγονται την τρίτη**:

1. Το $G$ είναι **συνεκτικό**.
2. Το $G$ είναι **ακυκλικό**.
3. Το $G$ έχει ακριβώς **$n - 1$ ακμές**.

Και όποιος συνδυασμός δύο κι αν ισχύει, το $G$ είναι **δένδρο**. Με άλλα λόγια: το δένδρο είναι το «οριακό» γράφημα — το ελάχιστο συνεκτικό, ή ισοδύναμα το μέγιστο ακυκλικό. Μία ακμή λιγότερη και διασπάται· μία παραπάνω και αποκτά κύκλο.
</Callout>

«Οποιεσδήποτε δύο» ακούγεται σαν ταχυδακτυλουργία — μέχρι να το δοκιμάσεις. Στο εργαλείο παρακάτω ξεκινάς από ένα δένδρο 6 κορυφών (και οι τρεις δείκτες πράσινοι). Κάνε **ένα** πειραματικό κλικ:

- Πρόσθεσε μία ακμή → εμφανίζεται κύκλος, οπότε «ακυκλικό» πέφτει στο κόκκινο. Και το πλήθος ακμών γίνεται $n$ → «$n-1$» πέφτει επίσης. Δεν γίνεται να σπάσει μόνο μία από τις τρεις.
- Αφαίρεσε μία ακμή του δένδρου → το γράφημα σπάει σε δύο κομμάτια, «συνεκτικό» πέφτει. Και το πλήθος γίνεται $n-2$, «$n-1$» πέφτει κι αυτό.

Το πρότυπο «προσπάθησε να σπάσεις μόνο μία ⇒ σπάει και άλλη» **είναι** η απόδειξη του θεωρήματος, μεταμφιεσμένη σε παιχνίδι:

<TreeThreeProperties />

**Μια συνέπεια που θα χρησιμοποιήσουμε ξανά και ξανά:** σε ένα δένδρο υπάρχει **μοναδικό μονοπάτι** ανάμεσα σε κάθε ζευγάρι κορυφών. Αν υπήρχαν δύο διαφορετικά μονοπάτια ανάμεσα σε δύο κορυφές, ενώνοντάς τα θα σχηματιζόταν κύκλος — αντίφαση με το ότι το δένδρο είναι ακυκλικό.

### Δένδρα με ρίζα

Μέχρι τώρα μιλήσαμε για δένδρα ως απλά γραφήματα — όλες οι κορυφές «ισότιμες». Συχνά όμως διαλέγουμε **μία** κορυφή ως ξεχωριστή και «κρεμάμε» από αυτήν όλο το δένδρο. Αυτή η κορυφή λέγεται **ρίζα** (root), και η επιλογή της δημιουργεί **ιεραρχία**: κοντά στη ρίζα = «ψηλά», μακριά της = «χαμηλά».

Με τη ρίζα διαλεγμένη, κάθε κορυφή $v$ αποκτά τρεις νέους ρόλους:

- **Γονέας (parent)** — ο μοναδικός γείτονας της $v$ που είναι **πιο κοντά** στη ρίζα. (Η ρίζα δεν έχει γονέα.)
- **Παιδιά (children)** — οι γείτονες που είναι πιο **μακριά** από τη ρίζα.
- Αν δεν έχει παιδιά, η $v$ είναι **φύλλο (leaf)**.

Το κλειδί που πολλοί μαθητές χάνουν: **το δένδρο μένει το ίδιο, η ρίζα είναι επιλογή σου**. Διαφορετική ρίζα → διαφορετικός γονέας, διαφορετικά παιδιά, διαφορετικά φύλλα — από τις *ίδιες ακριβώς ακμές*. Άλλαξε ρίζα με κλικ και δες το να ξανακρεμιέται:

<RootedTreeReroot />

Δένδρα με ρίζα είναι παντού στην πληροφορική: φυλογενετικά δένδρα στη βιολογία, η δομή των αρχείων ενός δίσκου, το DOM μιας ιστοσελίδας, το αφηρημένο συντακτικό δένδρο (AST) ενός προγράμματος — ακόμα και τα δένδρα αναδρομής που σχεδιάσαμε στο [L03](/lectures/L03-divide-and-conquer-i).

## Τι θα ρωτήσουμε — τα προβλήματα των L07–L09

Τώρα που έχουμε τη γλώσσα, ας **διατυπώσουμε** τα προβλήματα. Αυτά είναι που θα μας απασχολήσουν στις επόμενες διαλέξεις:

> **Πρόβλημα 1 — s-t συνεκτικότητα.** Δεδομένων δύο κορυφών $s$ και $t$, υπάρχει διαδρομή από την $s$ στην $t$;

> **Πρόβλημα 2 — s-t απόσταση.** Δεδομένων δύο κορυφών $s$ και $t$, ποιο είναι το **μήκος** της συντομότερης διαδρομής από την $s$ στην $t$;

Μοιάζουν απλά, αλλά κρύβονται παντού:

- **Κοινωνικά δίκτυα** — πόσο «μακριά» είναι δύο άνθρωποι;
- **Λαβύρινθος** — υπάρχει διαδρομή από την είσοδο στην έξοδο;
- **Six Degrees of Kevin Bacon** — πόσες ταινίες απέχει ένας ηθοποιός από τον Kevin Bacon;
- **Routing στο διαδίκτυο** — το πρωτόκολλο OSPF βρίσκει συντομότερες διαδρομές σε δίκτυα.

<Callout type="key">
**Ο οδικός χάρτης.** Στο [L07](/lectures/L07-graphs-ii) θα λύσουμε ακριβώς αυτά τα δύο προβλήματα με **BFS** και **DFS**. Στο [L08](/lectures/L08-graphs-iii) θα περάσουμε σε **κατευθυνόμενα** γραφήματα, με διμερότητα και ισχυρή συνεκτικότητα. Στο [L09](/lectures/L09-graphs-iv) θα προσθέσουμε **βάρη** στις ακμές — Dijkstra και Bellman-Ford για συντομότερες διαδρομές, Prim και Kruskal για ελάχιστα συνδετικά δένδρα.
</Callout>

## Κλείδωσε τη γνώση

<RecallCard
  algorithm="Δένδρο — οι τρεις ιδιότητες"
  keywords={['συνεκτικό', 'ακυκλικό', 'n−1 ακμές', 'οποιεσδήποτε δύο ⇒ η τρίτη', 'μοναδικό μονοπάτι']}
  skeleton={[
    'Δένδρο = συνεκτικό + ακυκλικό. Έχει ακριβώς n − 1 ακμές.',
    'Θεώρημα: από τις τρεις ιδιότητες (συνεκτικό / ακυκλικό / n−1 ακμές), όποιες δύο ισχύουν συνεπάγονται την τρίτη.',
    'Πρόσθεσε μία ακμή στο δένδρο → δημιουργείται κύκλος (χάνει το «ακυκλικό»).',
    'Αφαίρεσε μία ακμή στο δένδρο → σπάει σε δύο συνιστώσες (χάνει το «συνεκτικό»).',
    'Σε δένδρο: μοναδικό μονοπάτι ανάμεσα σε κάθε ζεύγος κορυφών.',
  ]}
  complexity="n κορυφές, n−1 ακμές"
  trap="«n−1 ακμές» από μόνο του ΔΕΝ φτιάχνει δένδρο: μπορείς να έχεις n−1 ακμές με κύκλο σε ένα κομμάτι και μια απομονωμένη κορυφή σε άλλο. Χρειάζεσαι ΔΥΟ από τις τρεις."
/>

Συμπλήρωσε τα κενά στις θεμελιώδεις έννοιες — χωρίς να κοιτάς πίσω:

<ClozeDrill
  prompt="Το λεξιλόγιο των γραφημάτων, σε τέσσερις λέξεις-κλειδιά."
  segments={[
    { text: 'Ένα γράφημα G = (V, E) έχει n κορυφές και m ' },
    { blank: 'ακμές', hint: 'edges' },
    { text: '. Το άθροισμα των βαθμών όλων των κορυφών ισούται με ' },
    { blank: 'δύο', hint: 'πόσες φορές το m;' },
    { text: ' φορές το πλήθος των ακμών. Ένα συνεκτικό και ακυκλικό γράφημα λέγεται ' },
    { blank: 'δένδρο', hint: 'tree' },
    { text: ' και έχει ακριβώς n − ' },
    { blank: '1' },
    { text: ' ακμές, με μοναδικό μονοπάτι ανάμεσα σε κάθε ζεύγος κορυφών.' },
  ]}
/>

## Μοτίβο σκέψης

<ThinkingPattern
  title="Μοντελοποίηση προβλήματος ως γράφημα"
  signals={['σχέσεις ή συνδέσεις μεταξύ αντικειμένων', 'δίκτυο / χάρτης / εξαρτήσεις', 'ποιο φτάνει σε ποιο']}
>
Πολλά προβλήματα δεν «μοιάζουν» με γραφήματα — γίνονται, μόλις κάνεις δύο αποφάσεις:

1. **Τι είναι οι κορυφές;** Συνήθως τα αντικείμενα ανάμεσα στα οποία υπάρχει η σχέση που σε ενδιαφέρει.
2. **Τι είναι οι ακμές;** Η ίδια η σχέση. Ρώτα επίσης: είναι **συμμετρική** (μη κατευθυνόμενη ακμή) ή όχι (κατευθυνόμενη, [L08](/lectures/L08-graphs-iii)); έχει **βάρος** (κόστος/απόσταση, [L09](/lectures/L09-graphs-iv)) ή όχι;

Η ίδια κατάσταση μπορεί να δώσει **διαφορετικό** γράφημα ανάλογα με την ερώτηση — θυμήσου το μετρό: σταθμοί-ως-κορυφές για να μετρήσεις στάσεις, γραμμές-ως-κορυφές για να μετρήσεις αλλαγές. Μόλις το πρόβλημα γίνει γράφημα, οι αλγόριθμοι των L07–L09 το λύνουν **έτοιμοι** — η δουλειά σου είναι η μετάφραση.
</ThinkingPattern>

<ExamRadar
  items={[
    {
      topic: 'Μοντελοποίηση μιας περιγραφής ως γράφημα — τι είναι κορυφές, τι ακμές',
      likelihood: 'medium',
      note: 'Σπάνια αυτόνομο θέμα· συνήθως το πρώτο βήμα ενός μεγαλύτερου προβλήματος γραφημάτων.',
      relatedExerciseIds: ['front-set-7-ask2'],
    },
    {
      topic: 'Ιδιότητες δένδρων — το θεώρημα «δύο από τις τρεις» (συνεκτικό / ακυκλικό / n−1 ακμές)',
      likelihood: 'medium',
    },
    {
      topic: 'Υπολογισμός βαθμών και χρήση του λήμματος της χειραψίας Σ deg = 2m',
      likelihood: 'low',
    },
    {
      topic: 'Σύγκριση αναπαραστάσεων (πίνακας vs λίστα) και κόστος βασικών πράξεων',
      likelihood: 'medium',
      note: 'Συχνά μέρος μεγαλύτερου ερωτήματος — «δώσε αλγόριθμο και πολυπλοκότητα» όπου το ζητούμενο κρύβεται στην επιλογή αναπαράστασης.',
      relatedExerciseIds: ['pt6-th1'],
    },
    {
      topic: 'Συνεκτικές συνιστώσες με επαναλαμβανόμενο BFS/DFS — αλγόριθμος και πολυπλοκότητα Θ(|V|+|E|)',
      likelihood: 'high',
      note: 'Σταθερό μοτίβο εξετάσεων: εξωτερικός βρόχος που οδηγεί BFS από κάθε ασημάδευτη κορυφή· βέλτιστος γραμμικός χρόνος.',
      relatedExerciseIds: ['pt5-th1', 'front-set-5-ask5'],
    },
    {
      topic: 'Επαναληπτική αφαίρεση κορυφών βάσει βαθμού (peeling, k-core, φιλτράρισμα)',
      likelihood: 'medium',
      note: 'Σχήμα «αν παραβιάζει τώρα, δεν ανήκει σε καμία έγκυρη λύση» — βάση πολλών «μέγιστο υποσύνολο» προβλημάτων.',
      relatedExerciseIds: ['front-set-7-ask9'],
    },
  ]}
/>

<Recap>
**Τι κρατάμε από αυτή τη διάλεξη**

1. **Γράφημα $G = (V, E)$** — $n$ κορυφές, $m$ ακμές. Ο βαθμός μιας κορυφής μετράει τους γείτονές της, και $\sum \deg(v) = 2m$ (χειραψία).
2. **Δύο αναπαραστάσεις:** πίνακας γειτνίασης ($\Theta(n^2)$ χώρος, $O(1)$ έλεγχος ακμής) ή λίστα γειτνίασης ($\Theta(n+m)$ χώρος, $O(\deg)$ σάρωση γειτόνων). Λίστα για αραιά γραφήματα — το σύνηθες· πίνακας για πυκνά.
3. **Έννοιες κίνησης:** διαδρομή, απλή διαδρομή, μήκος, συνεκτικότητα, συνεκτική συνιστώσα, κύκλος.
4. **Δένδρο:** συνεκτικό + ακυκλικό + $n-1$ ακμές — οποιεσδήποτε δύο δίνουν την τρίτη. Μοναδικό μονοπάτι ανάμεσα σε κάθε ζεύγος κορυφών.
5. **Δένδρο με ρίζα:** ιεραρχία γονέα–παιδιού–φύλλου. Παντού: AST, DOM, δομές αρχείων, δένδρα αναδρομής.
6. **Τα προβλήματα προς λύση:** s-t συνεκτικότητα και s-t απόσταση — τα λύνουν το **BFS** και το **DFS** στο [L07](/lectures/L07-graphs-ii).
</Recap>

<NextUp current="lectures/L06-graphs-i" />

<LectureExercises lectureSlug="lectures/L06-graphs-i" />
