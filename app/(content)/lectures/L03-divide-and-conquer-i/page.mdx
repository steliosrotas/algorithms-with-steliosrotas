export const metadata = {
  title: 'L03 · D&C I — Mergesort & Master Theorem',
  description:
    'Η πρώτη τεχνική σχεδίασης αλγορίθμων: διαίρει και κυρίευε. Mergesort βήμα-βήμα, τέσσερις αποδείξεις του O(n log n), ο πύργος του Hanoi, και το Master Theorem που λύνει αναδρομικές σχέσεις σε τρεις γραμμές.',
}

export const frontmatter = {
  title: 'L03 · D&C I — Mergesort & Master Theorem',
  slug: 'lectures/L03-divide-and-conquer-i',
  order: 3,
  prerequisites: ['lectures/L02-asymptotic-analysis'],
  examWeight: 8,
  estimatedReadTime: 55,
  lastUpdated: '2026-05-23',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Divide &amp; Conquer</span>
  <span>·</span>
  <span>Διάλεξη 3</span>
  <span>·</span>
  <span>~55 min</span>
</div>

# L03 · D&C I — Mergesort &amp; Master Theorem

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="lectures/L03-divide-and-conquer-i" />
  <Bookmark id="lectures/L03-divide-and-conquer-i" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc
  sources={{ pdf: 'L03 - Διαίρει και Κυρίευε Ι (Συγχωνευτική Ταξινόμηση, Αναδρομικές Σχέσεις και Master Theorem).pdf' }}
  note="Η σελίδα καλύπτει όλη τη διάλεξη (111 διαφάνειες)."
/>

## Τι θα δούμε

Στα [L01](/lectures/L01-eisagogika) και [L02](/lectures/L02-asymptotic-analysis) μάθαμε να **αποτιμούμε** και να **συγκρίνουμε** αλγορίθμους. Από εδώ και πέρα αλλάζουμε ερώτηση: πώς **σχεδιάζουμε** έναν καλό αλγόριθμο από το μηδέν;

Αυτή η διάλεξη είναι η **πρώτη τεχνική σχεδίασης** του μαθήματος. Στόχοι:

1. Να καταλάβουμε το **«διαίρει και κυρίευε»** ως *σχήμα* — όχι έναν αλγόριθμο, αλλά μια ολόκληρη οικογένεια αλγορίθμων που χτίζονται με τον ίδιο τρόπο.
2. Να δούμε το αστέρι της οικογένειας, τη **συγχωνευτική ταξινόμηση (mergesort)**, και να αποδείξουμε ότι τρέχει σε $O(n \log n)$ — με **τέσσερις διαφορετικούς τρόπους**, γιατί καθένας διδάσκει μια χρήσιμη τεχνική.
3. Να βγάλουμε ένα γενικό εργαλείο, το **Master Theorem**, που λύνει μια ολόκληρη οικογένεια αναδρομικών σχέσεων χωρίς απόδειξη από την αρχή.
4. Να δούμε, με τον **πύργο του Hanoi**, ότι το σχήμα D&C *από μόνο του* δεν εγγυάται γρήγορο αλγόριθμο.

## Γρήγορη υπενθύμιση από το L02

Όλη η διάλεξη μιλάει για χρόνους εκτέλεσης, οπότε ας ξανασυμφωνήσουμε στη γλώσσα. Από το [L02](/lectures/L02-asymptotic-analysis):

- $T(n) = O(f(n))$ — **άνω φράγμα**: η $T$ δεν αυξάνεται γρηγορότερα από την $f$.
- $T(n) = \Omega(f(n))$ — **κάτω φράγμα**: δεν αυξάνεται πιο αργά.
- $T(n) = \Theta(f(n))$ — **ακριβές φράγμα**: ίδιος ρυθμός, και από τις δύο μεριές.

Και η **κλίμακα πολυπλοκοτήτων**, που θα χρησιμοποιήσουμε διαρκώς:

$$1 \prec \log n \prec n \prec n \log n \prec n^2 \prec n^3 \prec \dots \prec 2^n \prec n!$$

Όσα είναι αριστερά (**πολυωνυμικά**) είναι «καλά»· όσα είναι δεξιά (**εκθετικά**) είναι «κακά». Κράτα ειδικά στο μυαλό σου τη θέση του $n \log n$ — **ανάμεσα** στο γραμμικό και στο τετραγωνικό. Εκεί ζει η mergesort.

<Callout type="key">
**Βέλτιστος αλγόριθμος** (επανάληψη από L02). Αν για ένα πρόβλημα αποδεικνύεται **κάτω φράγμα** $\Omega(f(n))$ — δηλαδή ότι *κάθε* αλγόριθμος που το λύνει χρειάζεται τουλάχιστον τόσα βήματα — και έχουμε αλγόριθμο σε $O(f(n))$, τότε ο αλγόριθμός μας είναι **ασυμπτωτικά βέλτιστος**. Στο τέλος της διάλεξης θα δούμε ότι η mergesort είναι ακριβώς τέτοια περίπτωση.
</Callout>

## Ζέσταμα: το μέγιστο ενός πίνακα

Πριν επιτεθούμε στο σύνθετο πρόβλημα της ταξινόμησης, ένα μίνι πρόβλημα για να ξαναβρούμε τον ρυθμό της ανάλυσης. **Δοθέντος πίνακα $a_1, a_2, \dots, a_n$, βρες το μέγιστο στοιχείο.**

Η πιο φυσική ιδέα: κράτα έναν «τρέχοντα νικητή» και διέτρεξε τον πίνακα μία φορά.

<Pseudocode
  title="Ψευδοκώδικας — μέγιστο στοιχείο"
  defaultOpen
  code={`μέγιστο(a[1..n]):
  v ← a[1]
  για i = 2 έως n:
    αν v < a[i]:  v ← a[i]
  επίστρεψε v`}
  caption="Στοιχειώδης πράξη: η σύγκριση v < a[i]."
/>

Πόσες συγκρίσεις; **Ακριβώς $n-1$** — μία για κάθε στοιχείο πέρα από το πρώτο, ανεξάρτητα από την είσοδο. Άρα $\Theta(n)$.

Υπάρχει και μια δεύτερη ιδέα: σάρωσε τα γειτονικά ζευγάρια κι **αντάλλασσε** ώστε το μεγαλύτερο να «σπρώχνεται» προς το τέλος· στο τέλος, το $a_n$ είναι το μέγιστο. Πάλι $n-1$ συγκρίσεις, πάλι $\Theta(n)$.

<Callout type="note">
Οι δύο λύσεις έχουν **την ίδια ασυμπτωτική πολυπλοκότητα**, αλλά η πρώτη είναι σαφώς προτιμότερη: η δεύτερη **τροποποιεί** τον πίνακα εισόδου. Όταν δύο αλγόριθμοι είναι ισόπαλοι ασυμπτωτικά, η επιλογή κρίνεται σε **άλλα κριτήρια**: μνήμη, αν αλλοιώνουν την είσοδο, ευκολία υλοποίησης, σταθερότητα.
</Callout>

## Το πρόβλημα: ταξινόμηση

> **Πρόβλημα.** Δοθέντος πίνακα στοιχείων, να τα διατάξουμε σε αύξουσα σειρά (ως προς κάποιο κριτήριο σύγκρισης).

Γιατί ξοδεύουμε ολόκληρη διάλεξη στην ταξινόμηση; Γιατί είναι **παντού**. Οι προφανείς χρήσεις — μια αλφαβητική λίστα, μια playlist, αρχεία ταξινομημένα κατά ημερομηνία — είναι μόνο η αρχή. Το πραγματικό κέρδος είναι ότι μια **ταξινομημένη** λίστα ξεκλειδώνει δεκάδες άλλα προβλήματα:

- **Δυαδική αναζήτηση** σε $O(\log n)$ αντί για $O(n)$ — όπως είδαμε στο [L01](/lectures/L01-eisagogika).
- **Μεσαίο στοιχείο** σε $O(1)$, **ακραίες τιμές** στις άκρες, **διπλότυπα** σε ένα γραμμικό πέρασμα (τα ίσα στοιχεία στέκονται πλέον δίπλα-δίπλα).
- **Πλησιέστερο ζευγάρι αριθμών**: σε ταξινομημένη λίστα είναι κάποιο γειτονικό ζευγάρι — $O(n)$.

Και αμέτρητα προβλήματα χρησιμοποιούν την ταξινόμηση ως **κρυφό υποπρόβλημα**: συμπίεση δεδομένων (Huffman, [L12](/lectures/L12-greedy-ii)), γραφικά, υπολογιστική βιολογία, ο αλγόριθμος Kruskal για ελάχιστα συνδετικά δέντρα ([L09](/lectures/L09-graphs-iv)) που απαιτεί ταξινομημένες ακμές.

<Callout type="intuition">
Η ταξινόμηση είναι από τα **πιο μελετημένα προβλήματα** της επιστήμης υπολογιστών ακριβώς γι' αυτόν τον λόγο: ένας γρήγορος αλγόριθμος ταξινόμησης κάνει γρηγορότερα και όλα όσα τη χρησιμοποιούν από κάτω.
</Callout>

## Πρώτη προσπάθεια: bubble &amp; insertion sort

Πριν δούμε το «έξυπνο» mergesort, ας δούμε τι μας δίνει η πιο αφελής σκέψη — δύο κλασικοί απλοί αλγόριθμοι.

<Algorithm
  name="Bubble sort — «η φυσαλίδα»"
  idea="Σε κάθε πέρασμα, το μεγαλύτερο στοιχείο «αναδύεται» σαν φυσαλίδα στη σωστή θέση στο τέλος."
  complexity="Θ(n²)"
>

**Με λόγια.** Διατρέχεις τον πίνακα συγκρίνοντας γειτονικά ζευγάρια· όποτε βρεις δύο σε λάθος σειρά, τα αντάλλασσεις. Μετά από ένα πλήρες πέρασμα, το μεγαλύτερο στοιχείο έχει «κυλήσει» μέχρι το τέλος. Επαναλαμβάνεις για το υπόλοιπο — μετά από $n-1$ περάσματα όλα είναι στη θέση τους.

<Pseudocode
  code={`bubblesort(a[1..n]):
  για i = 1 έως n − 1:
    για j = 1 έως n − i:
      αν a[j] > a[j+1]:  αντάλλαξε a[j] ↔ a[j+1]`}
/>

**Πολυπλοκότητα.** Διπλά φωλιασμένος βρόχος: $(n-1) + (n-2) + \dots + 1 = \dfrac{n(n-1)}{2} = \Theta(n^2)$.

</Algorithm>

<Algorithm
  name="Insertion sort — «η ενθετική»"
  idea="Όπως ταξινομείς τραπουλόχαρτα στο χέρι: κάθε νέο φύλλο μπαίνει στη σωστή θέση ανάμεσα στα ήδη ταξινομημένα."
  complexity="Θ(n²)"
>

**Με λόγια.** Κρατάς το αριστερό κομμάτι του πίνακα πάντα ταξινομημένο. Παίρνεις το επόμενο στοιχείο και το «κατεβάζεις» προς τα αριστερά, ολισθαίνοντας τα μεγαλύτερα στοιχεία ένα βήμα δεξιά, ώσπου να βρει τη θέση του.

<Pseudocode
  code={`insertionsort(a[1..n]):
  για i = 2 έως n:
    v ← a[i]
    j ← i − 1
    όσο j ≥ 1 και v < a[j]:
      a[j+1] ← a[j]          // ολίσθηση δεξιά
      j ← j − 1
    a[j+1] ← v`}
/>

**Πολυπλοκότητα.** Στη **χείριστη** περίπτωση (αντίστροφα ταξινομημένος πίνακας) κάθε νέο στοιχείο κατεβαίνει σε όλο το ταξινομημένο πρόθεμα → $\Theta(n^2)$. Στη **βέλτιστη** περίπτωση (ήδη ταξινομημένος) κάθε στοιχείο μένει επιτόπου → μόνο $\Theta(n)$.

</Algorithm>

<Callout type="warning">
Και οι δύο έχουν **χείριστη πολυπλοκότητα $\Theta(n^2)$**. Για $n = 10^6$ στοιχεία αυτό σημαίνει $\sim 10^{12}$ πράξεις — χρόνος της τάξης της **ώρας**. Η mergesort που έρχεται κάνει την ίδια δουλειά σε $\sim n \log n \approx 2 \cdot 10^7$ πράξεις — **δευτερόλεπτα**. Η διαφορά $n^2$ έναντι $n \log n$ είναι εδώ **πέντε τάξεις μεγέθους**.
</Callout>

Πριν δούμε γιατί, ας τη **νιώσουμε**. Πατώντας ▶ τρέχεις και τους τρεις αλγορίθμους στον ίδιο πίνακα 10 στοιχείων· κάθε αλγόριθμος έχει τον δικό του μετρητή συγκρίσεων, και βλέπεις πόσο γρηγορότερα γεμίζει ο της mergesort. Άλλαξε σε «Αντίστροφος» για να δεις τη **χειρότερη** είσοδο της insertion (όπου ισοφαρίζει την bubble) και σε «Σχεδόν ταξινομημένος» για τη **βέλτιστη** (όπου η insertion γίνεται σχεδόν γραμμική):

<SortRace />

Δύο πράγματα να προσέξεις. Η **bubble sort είναι ανεπηρέαστη**: 45 συγκρίσεις και στις τρεις περιπτώσεις, γιατί ο εξωτερικός βρόχος δεν «βλέπει» τι έγινε μέσα. Η **insertion sort είναι έξυπνη**: για σχεδόν-ταξινομημένη είσοδο τελειώνει σε πολύ λιγότερες συγκρίσεις. Αλλά **καμιά από τις δύο** δεν είναι κοντά στη mergesort όταν η είσοδος είναι τυχαία ή αντίστροφη — και η απόσταση **ανοίγει** όσο μεγαλώνει το $n$, ακριβώς όπως λέει το $n^2$ vs $n \log n$.

## Η μεγάλη ιδέα: διαίρει και κυρίευε

Πρέπει να σπάσουμε το φράγμα του $\Theta(n^2)$. Από πού πιάνεται κανείς; Από μια πολύ ανθρώπινη ιδέα.

<Callout type="intuition">
Φαντάσου ότι πρέπει να ταξινομήσεις αλφαβητικά μια **τεράστια στοίβα γραπτών**. Μόνος, είναι αργό και κουραστικό. Αλλά: κόψε τη στοίβα στη μέση και δώσε το μισό σε έναν φίλο. Τώρα ταξινομείτε **παράλληλα** δύο μικρότερες στοίβες. Όταν τελειώσετε, έχετε δύο **ταξινομημένες** στοίβες — και το να τις ενώσεις σε μία είναι εύκολο: κοιτάς μόνο τα δύο πάνω-πάνω γραπτά κάθε φορά και τραβάς το «μικρότερο». Κι ο φίλος σου; Κάνει το ίδιο ακριβώς κόλπο — κόβει τη δική του στοίβα στη μέση και τη μοιράζει παρακάτω. Αυτό είναι το **«διαίρει και κυρίευε»**, κι αυτός είναι, σχεδόν αυτολεξεί, ο αλγόριθμος mergesort.
</Callout>

> ### Divide et impera!
>
> *(λατινικό: «διαίρει και βασίλευε»)*

Η τεχνική **διαίρει και κυρίευε (D&C)** βασίζεται σε τρία βήματα:

1. **Διαίρει** τα δεδομένα σε μικρότερα υποπροβλήματα **χωρίς κοινά στοιχεία** (ξένα μεταξύ τους).
2. **Κυρίευσε** κάθε υποπρόβλημα **ξεχωριστά** και **αναδρομικά** — εφαρμόζοντας ξανά τον ίδιο αλγόριθμο.
3. **Συνδύασε** τις επιμέρους λύσεις σε λύση του αρχικού προβλήματος.

<svg viewBox="0 0 720 280" className="my-4 w-full max-w-3xl" role="img" aria-label="Σχηματική παράσταση διαίρει και κυρίευε">
  <style>{`
    .dc-node { fill: rgb(var(--bg-elevated)); stroke: rgb(var(--accent)); stroke-width: 1.5; }
    .dc-root { fill: rgb(var(--accent) / 0.18); stroke: rgb(var(--accent)); stroke-width: 2; }
    .dc-arrow { stroke: rgb(var(--border-strong)); stroke-width: 1.5; fill: none; }
    .dc-label { font-family: ui-sans-serif, sans-serif; font-size: 13px; font-weight: 600; fill: rgb(var(--fg)); }
    .dc-sub { font-family: ui-sans-serif, sans-serif; font-size: 11px; fill: rgb(var(--fg-muted)); font-style: italic; }
    .dc-step { font-family: ui-sans-serif, sans-serif; font-size: 11px; font-weight: 700; fill: rgb(var(--accent)); }
  `}</style>
  <g>
    <rect x="280" y="20" width="160" height="46" rx="8" className="dc-root" />
    <text x="360" y="42" textAnchor="middle" className="dc-label">Πρόβλημα μεγέθους n</text>
    <text x="360" y="58" textAnchor="middle" className="dc-sub">(αρχική είσοδος)</text>
  </g>
  <text x="100" y="100" className="dc-step">① Διαίρει</text>
  <line x1="320" y1="66" x2="120" y2="110" className="dc-arrow" />
  <line x1="360" y1="66" x2="360" y2="110" className="dc-arrow" />
  <line x1="400" y1="66" x2="600" y2="110" className="dc-arrow" />
  <g>
    <rect x="40" y="110" width="160" height="46" rx="8" className="dc-node" />
    <text x="120" y="132" textAnchor="middle" className="dc-label">Υποπρόβλημα 1</text>
    <text x="120" y="148" textAnchor="middle" className="dc-sub">μεγέθους n/2</text>
  </g>
  <g>
    <rect x="280" y="110" width="160" height="46" rx="8" className="dc-node" />
    <text x="360" y="132" textAnchor="middle" className="dc-label">Υποπρόβλημα 2</text>
    <text x="360" y="148" textAnchor="middle" className="dc-sub">μεγέθους n/2</text>
  </g>
  <g>
    <rect x="520" y="110" width="160" height="46" rx="8" className="dc-node" />
    <text x="600" y="132" textAnchor="middle" className="dc-label">…</text>
    <text x="600" y="148" textAnchor="middle" className="dc-sub">μικρότερου μεγέθους</text>
  </g>
  <text x="100" y="190" className="dc-step">② Κυρίευε (αναδρομικά)</text>
  <line x1="120" y1="156" x2="120" y2="200" className="dc-arrow" />
  <line x1="360" y1="156" x2="360" y2="200" className="dc-arrow" />
  <line x1="600" y1="156" x2="600" y2="200" className="dc-arrow" />
  <g>
    <rect x="40" y="200" width="160" height="36" rx="8" className="dc-node" />
    <text x="120" y="222" textAnchor="middle" className="dc-label">Λύση 1</text>
  </g>
  <g>
    <rect x="280" y="200" width="160" height="36" rx="8" className="dc-node" />
    <text x="360" y="222" textAnchor="middle" className="dc-label">Λύση 2</text>
  </g>
  <g>
    <rect x="520" y="200" width="160" height="36" rx="8" className="dc-node" />
    <text x="600" y="222" textAnchor="middle" className="dc-label">…</text>
  </g>
  <text x="100" y="265" className="dc-step">③ Συνδύασε</text>
  <line x1="120" y1="236" x2="320" y2="265" className="dc-arrow" />
  <line x1="360" y1="236" x2="360" y2="265" className="dc-arrow" />
  <line x1="600" y1="236" x2="400" y2="265" className="dc-arrow" />
  <g>
    <rect x="280" y="265" width="160" height="12" className="dc-root" rx="6" />
  </g>
</svg>

### Γιατί δουλεύει: είναι επαγωγή μεταμφιεσμένη

Η D&C δεν είναι ένα κόλπο που «τυχαίνει» να δουλεύει — είναι **επαγωγική κατασκευή της λύσης**. Για να σχεδιάσεις έναν τέτοιο αλγόριθμο, αρκεί (και χρειάζεται) να ξέρεις δύο πράγματα:

1. **Βάση:** πώς λύνεται το πρόβλημα για **πολύ μικρή, σταθερή** είσοδο — π.χ. $n = 1$.
2. **Επαγωγικό βήμα:** πώς **συνδυάζονται** οι ορθές λύσεις των μικρότερων υποπροβλημάτων σε μία ορθή λύση για όλη την είσοδο.

<Callout type="key">
**Η σύνδεση με τη μαθηματική επαγωγή είναι κυριολεκτική.** Αν αποδείξεις (α) ότι ο αλγόριθμος είναι σωστός για $n = 1$ και (β) ότι, *υποθέτοντας* πως είναι σωστός για κάθε $k < n$, είναι σωστός και για $n$ — τότε **έχεις απόδειξη ορθότητας**. Αυτό είναι το πρότυπο απόδειξης για κάθε αλγόριθμο D&C, και θα το χρησιμοποιήσουμε σε όλη τη διάλεξη.
</Callout>

<ThinkingPattern
  title="Πότε να σκεφτείς «διαίρει και κυρίευε»"
  signals={['ταξινόμησε / μέτρησε / βρες σε μεγάλο πίνακα', 'το πρόβλημα σπάει σε ανεξάρτητα κομμάτια', 'σχεδίασε αποδοτικό αλγόριθμο']}
>
Αναγνωρίζεις ένα πρόβλημα-D&C όταν ισχύουν **και τα τρία**:

1. Η είσοδος **σπάει** σε δύο (ή λίγα) μικρότερα στιγμιότυπα **του ίδιου προβλήματος**.
2. Τα κομμάτια είναι **ανεξάρτητα** — λύνεις το καθένα χωρίς να κοιτάς τα άλλα.
3. Οι λύσεις των κομματιών **συνδυάζονται φτηνά** σε λύση του συνόλου.

Το τρίτο σημείο είναι το κρίσιμο. Αν ο συνδυασμός είναι ακριβός, το D&C δεν κερδίζει τίποτα — θα το δούμε ολοκάθαρα στον πύργο του Hanoi παρακάτω.
</ThinkingPattern>

## Συγχωνευτική ταξινόμηση: η ιδέα

Ας ντύσουμε την αναλογία της στοίβας με γραπτά σε αλγόριθμο. Πώς ταξινομούμε με D&C;

1. **Βάση.** Πίνακας με **ένα στοιχείο** είναι ήδη ταξινομημένος — τον επιστρέφουμε ως έχει.
2. **Αλλιώς:** *διαίρεσε* τον πίνακα στη μέση· *ταξινόμησε* αναδρομικά καθένα από τα δύο μισά· *συγχώνευσε* τις δύο ταξινομημένες λίστες σε μία.

Όλο το βάρος της σκέψης είναι: «αν ξέρω να **συγχωνεύω** δύο ταξινομημένες λίστες, τότε ξέρω να ταξινομώ τα πάντα» — η αναδρομή αναλαμβάνει το υπόλοιπο. Πριν γράψουμε γραμμή ψευδοκώδικα, πάτα **Επόμενο** και δες το να συμβαίνει — πρώτα η φάση *Διαίρει* μέχρι τους πίνακες ενός στοιχείου, μετά η φάση *Συγχώνευσε* προς τα πάνω:

<MergeSortAnimator />

Πρόσεξε αυτό που μετράει το «Δουλειά συγχώνευσης ανά επίπεδο»: **κάθε** επίπεδο συγχώνευσης κοστίζει περίπου $n$ πράξεις, και τα επίπεδα είναι **λίγα** — όσα και τα μισαρίσματα μέχρι το 1. Κράτα το· είναι ολόκληρη η απόδειξη του $O(n \log n)$ σε μία εικόνα.

## Η συγχώνευση δύο ταξινομημένων πινάκων

Είδαμε ότι η mergesort στηρίζεται όλη πάνω σε μία πράξη: τη **συγχώνευση**. Ας τη χτίσουμε προσεκτικά — είναι το «επαγωγικό βήμα» του αλγορίθμου.

Σκέψου δύο ουρές στο supermarket που πρέπει να γίνουν μία ταξινομημένη ουρά: κρατάς δύο **δείκτες** $i$ και $j$, έναν στην αρχή κάθε ουράς, κοιτάς τα δύο πρώτα στοιχεία, παίρνεις το **μικρότερο** και προχωράς τον αντίστοιχο δείκτη. Επανάληψη, μέχρι να αδειάσουν και οι δύο ουρές. Πάτα **Επόμενο** και δες ακριβώς αυτό να συμβαίνει — ή πέρασε σε «Δοκίμασε εσύ» για να διαλέξεις μόνος σου ποια μεριά να τραβήξεις:

<TwoPointerMerge />

Δύο παρατηρήσεις που θα μας χρησιμεύσουν αμέσως μετά: (1) **κάθε βήμα προχωρά ακριβώς έναν δείκτη**, οπότε ο συνολικός αριθμός βημάτων είναι το πολύ $m + n$· (2) η συγχώνευση **εκμεταλλεύεται** το ότι οι δύο είσοδοι είναι ήδη ταξινομημένες — το μικρότερο στοιχείο που δεν έχει γραφτεί ακόμα είναι σίγουρα κάποιο από τα δύο «πάνω-πάνω».

<Algorithm
  name="Συγχώνευση δύο ταξινομημένων πινάκων"
  idea="Δύο δείκτες σαρώνουν τους δύο πίνακες από την αρχή· κάθε φορά τραβάμε στο αποτέλεσμα το μικρότερο από τα δύο «κορυφαία» στοιχεία."
  complexity="O(m + n)"
  io={{
    input: 'δύο ήδη ταξινομημένοι πίνακες a (m στοιχεία) και b (n στοιχεία)',
    output: 'ένας ταξινομημένος πίνακας c με όλα τα m + n στοιχεία',
  }}
>

**Με λόγια.** Κράτα έναν δείκτη $i$ στην αρχή του $a$ και έναν δείκτη $j$ στην αρχή του $b$. Κάθε δείκτης δείχνει το πρώτο στοιχείο που **δεν** έχεις χρησιμοποιήσει ακόμη. Σε κάθε βήμα κοιτάς τα δύο στοιχεία $a_i$ και $b_j$, παίρνεις το **μικρότερο** και το βάζεις στο αποτέλεσμα, και προχωράς **μόνο** τον δείκτη που χρησιμοποίησες. Επειδή και οι δύο πίνακες είναι ήδη ταξινομημένοι, το μικρότερο μη-χρησιμοποιημένο στοιχείο όλων είναι σίγουρα ένα από αυτά τα δύο. Σαν δύο ταξινομημένες ουρές στο supermarket που γίνονται μία.

<Pseudocode
  code={`συγχώνευση(a[1..m], b[1..n]):
  i ← 1 ;  j ← 1
  για k = 1 έως m + n:
    αν b εξαντλήθηκε, ή (a δεν εξαντλήθηκε και a[i] ≤ b[j]):
      c[k] ← a[i] ;  i ← i + 1
    αλλιώς:
      c[k] ← b[j] ;  j ← j + 1
  επίστρεψε c`}
  caption="Ο έλεγχος «εξαντλήθηκε» καλύπτει την περίπτωση που τελειώσει ο ένας πίνακας — τότε αντιγράφουμε ό,τι μένει από τον άλλον."
/>

**Πολυπλοκότητα.** Ο βρόχος εκτελείται ακριβώς $m+n$ φορές, και κάθε επανάληψη κάνει σταθερή δουλειά (μία σύγκριση, μία ανάθεση, μία αύξηση δείκτη). Άρα **$O(m+n)$** — γραμμικός στο σύνολο των στοιχείων.

</Algorithm>

<Callout type="note">
**Κόστος μνήμης.** Η συγχώνευση γράφει σε **νέο** πίνακα $c$ μεγέθους $m+n$ — δεν γίνεται «επιτόπου» (in-place) στην απλή της μορφή. Αυτό το επιπλέον $O(n)$ μνήμης είναι το μόνο μειονέκτημα της mergesort έναντι άλλων αλγορίθμων ταξινόμησης.
</Callout>

## Ο αλγόριθμος mergesort

Με τη συγχώνευση έτοιμη, ο πλήρης αλγόριθμος είναι σχεδόν τετριμμένος — γι' αυτό άλλωστε δουλεύει το D&C: μόλις λύσεις το «επαγωγικό βήμα», ο αλγόριθμος γράφεται μόνος του.

<Algorithm
  name="Mergesort (συγχωνευτική ταξινόμηση)"
  idea="Σπάσε στη μέση, ταξινόμησε αναδρομικά τα δύο μισά, συγχώνευσέ τα."
  complexity="O(n log n)"
  io={{
    input: 'πίνακας A και όρια p, r (αρχικά p = 1, r = n)',
    output: 'το τμήμα A[p..r] ταξινομημένο',
  }}
>

**Με λόγια.** Αν το τμήμα έχει ένα στοιχείο ($p = r$), είναι ήδη ταξινομημένο — αυτή είναι η **βάση** που σταματά την αναδρομή. Αλλιώς, βρίσκουμε το μεσαίο σημείο $q$, καλούμε αναδρομικά τη mergesort στο αριστερό και στο δεξί μισό, και τέλος συγχωνεύουμε τα δύο ταξινομημένα μισά. Την κάθε αναδρομική κλήση τη θεωρούμε **«μαύρο κουτί»**: εμπιστευόμαστε ότι επιστρέφει το μισό ταξινομημένο — αυτό ακριβώς εγγυάται η επαγωγική υπόθεση.

<Pseudocode
  code={`mergesort(A, p, r):
  αν p = r:
    επίστρεψε A[p]                       // βάση: ένα στοιχείο
  q ← ⌊(p + r) / 2⌋                       // σπάμε στη μέση
  mergesort(A, p, q)                      // ταξινόμησε το αριστερό μισό
  mergesort(A, q + 1, r)                  // ταξινόμησε το δεξί μισό
  επίστρεψε συγχώνευση(A[p..q], A[q+1..r])`}
  caption="Αρχική κλήση: mergesort(A, 1, n)."
/>

</Algorithm>

<Callout type="warning">
**Μια ερώτηση που θα απαντήσουμε σε λίγο.** Γιατί σπάμε **στη μέση**; Θα μπορούσαμε να διαιρέσουμε σε «$n-2$ και $2$» αντί για «$n/2$ και $n/2$». Κράτα την ερώτηση — η απάντηση («όχι, η πολυπλοκότητα καταρρέει») θα γίνει ολοφάνερη μόλις δούμε το δέντρο αναδρομής.
</Callout>

## Το δέντρο αναδρομής

Πριν αποδείξουμε τυπικά ότι $T(n) = O(n \log n)$, ας δούμε **πού πάει η δουλειά**. Ζωγραφίζουμε το **δέντρο αναδρομής**: η ρίζα είναι η αρχική κλήση, και κάθε κλήση κρεμάει από κάτω τις δύο αναδρομικές κλήσεις της. Για $n = 8$:

<svg viewBox="0 0 800 300" className="my-4 w-full max-w-3xl" role="img" aria-label="Δέντρο αναδρομής της mergesort για n=8">
  <style>{`
    .rt-node { fill: rgb(var(--bg-elevated)); stroke: rgb(var(--accent)); stroke-width: 1.5; }
    .rt-edge { stroke: rgb(var(--border-strong)); stroke-width: 1.2; fill: none; }
    .rt-text { font-family: ui-monospace, monospace; font-size: 11px; fill: rgb(var(--fg)); font-weight: 600; }
    .rt-level { font-family: ui-sans-serif, sans-serif; font-size: 11px; fill: rgb(var(--fg-muted)); font-style: italic; }
    .rt-cost { font-family: ui-monospace, monospace; font-size: 11px; fill: rgb(var(--accent)); font-weight: 700; }
  `}</style>
  <text x="20" y="30" className="rt-level">επίπεδο 0 (ρίζα)</text>
  <line x1="400" y1="40" x2="200" y2="80" className="rt-edge" />
  <line x1="400" y1="40" x2="600" y2="80" className="rt-edge" />
  <rect x="350" y="20" width="100" height="22" rx="4" className="rt-node" />
  <text x="400" y="36" textAnchor="middle" className="rt-text">n = 8</text>
  <text x="720" y="36" className="rt-cost">δουλειά: 8</text>
  <text x="20" y="90" className="rt-level">επίπεδο 1</text>
  <line x1="200" y1="100" x2="100" y2="140" className="rt-edge" />
  <line x1="200" y1="100" x2="300" y2="140" className="rt-edge" />
  <line x1="600" y1="100" x2="500" y2="140" className="rt-edge" />
  <line x1="600" y1="100" x2="700" y2="140" className="rt-edge" />
  <rect x="160" y="80" width="80" height="22" rx="4" className="rt-node" />
  <text x="200" y="96" textAnchor="middle" className="rt-text">n/2 = 4</text>
  <rect x="560" y="80" width="80" height="22" rx="4" className="rt-node" />
  <text x="600" y="96" textAnchor="middle" className="rt-text">n/2 = 4</text>
  <text x="720" y="96" className="rt-cost">2 × 4 = 8</text>
  <text x="20" y="150" className="rt-level">επίπεδο 2</text>
  <line x1="100" y1="160" x2="60" y2="200" className="rt-edge" />
  <line x1="100" y1="160" x2="140" y2="200" className="rt-edge" />
  <line x1="300" y1="160" x2="260" y2="200" className="rt-edge" />
  <line x1="300" y1="160" x2="340" y2="200" className="rt-edge" />
  <line x1="500" y1="160" x2="460" y2="200" className="rt-edge" />
  <line x1="500" y1="160" x2="540" y2="200" className="rt-edge" />
  <line x1="700" y1="160" x2="660" y2="200" className="rt-edge" />
  <line x1="700" y1="160" x2="740" y2="200" className="rt-edge" />
  {[100, 300, 500, 700].map((x) => (
    <g key={x}>
      <rect x={x - 30} y="140" width="60" height="22" rx="4" className="rt-node" />
      <text x={x} y="156" textAnchor="middle" className="rt-text">n/4 = 2</text>
    </g>
  ))}
  <text x="720" y="156" className="rt-cost">4 × 2 = 8</text>
  <text x="20" y="210" className="rt-level">επίπεδο 3 (φύλλα)</text>
  {[60, 140, 260, 340, 460, 540, 660, 740].map((x) => (
    <g key={x}>
      <rect x={x - 18} y="200" width="36" height="20" rx="4" className="rt-node" />
      <text x={x} y="215" textAnchor="middle" className="rt-text">1</text>
    </g>
  ))}
  <text x="720" y="215" className="rt-cost">8 × 1 = 8</text>
  <text x="20" y="260" className="rt-level">Βάθος δέντρου: log₂(n) = 3</text>
  <text x="20" y="278" className="rt-level">Σύνολο: 8 × log₂(8) = 24 πράξεις — γενικά n × log n</text>
</svg>

Το κλειδί είναι μία παρατήρηση: **κάθε επίπεδο του δέντρου κάνει συνολικά $n$ συγκρίσεις**. Γιατί;

- Στο επίπεδο 1 έχουμε 2 υποπροβλήματα μεγέθους $n/2$. Η συγχώνευση καθενός κοστίζει $n/2$ → σύνολο $n$.
- Στο επίπεδο 2 έχουμε 4 υποπροβλήματα μεγέθους $n/4$, κόστος $n/4$ το καθένα → σύνολο $n$.
- Σε γενικό επίπεδο $k$ έχουμε $2^k$ υποπροβλήματα μεγέθους $n/2^k$ → σύνολο πάλι $n$.

Τα υποπροβλήματα **πληθαίνουν**, αλλά ταυτόχρονα **μικραίνουν** στον ίδιο ρυθμό — οπότε το γινόμενο μένει σταθερό. Και πόσα επίπεδα υπάρχουν; Όσα τα μισαρίσματα του $n$ μέχρι το 1, δηλαδή $\log_2 n$. Άρα:

$$
T(n) = \underbrace{n}_{\text{ανά επίπεδο}} \times \underbrace{\log_2 n}_{\text{επίπεδα}} = n \log_2 n
$$

Αυτή είναι η **διαίσθηση**. Τώρα ας τη μετατρέψουμε σε αυστηρή απόδειξη — και μάλιστα με τέσσερις τρόπους.

## Η αναδρομική σχέση της mergesort

Ορίζουμε $T(n)$ ως την πολυπλοκότητα της mergesort για είσοδο μεγέθους $n$. Διαβάζοντας τον ψευδοκώδικα:

$$
T(n) = \begin{cases}
0 & \text{αν } n = 1 \\[4pt]
T(\lfloor n/2 \rfloor) + T(\lceil n/2 \rceil) + n & \text{αν } n > 1
\end{cases}
$$

Σε λόγια: για $n = 1$ καμία σύγκριση· για $n > 1$, το κόστος του αριστερού μισού **συν** του δεξιού μισού **συν** $n$ για τη συγχώνευση.

**Στόχος:** να δείξουμε ότι $T(n) = O(n \log n)$. Θα το κάνουμε **τέσσερις φορές**, με διαφορετική τεχνική κάθε φορά — κάθε τεχνική επιστρέφει σε άλλες αναδρομικές σχέσεις παρακάτω στο μάθημα.

<Callout type="note">
Στην εξέταση δεν χρειάζεται να ξέρεις και τις τέσσερις. Μάθε καλά **τουλάχιστον δύο** — ιδανικά την #1 (κομψή) και την #3 (γενικεύεται εύκολα).
</Callout>

## Απόδειξη #1 — διαίρεση με n (τηλεσκόπιο)

Υποθέτουμε ότι το $n$ είναι **δύναμη του 2**, οπότε δεν χρειάζονται πατώματα/οροφές και η σχέση γίνεται καθαρά:

$$
T(n) = 2\,T(n/2) + n
$$

**Το τέχνασμα:** διαιρούμε και τα δύο μέλη με $n$.

$$
\frac{T(n)}{n} = \frac{2\,T(n/2)}{n} + 1 = \frac{T(n/2)}{n/2} + 1
$$

Αν θέσουμε $U(n) = T(n)/n$, η σχέση απλοποιείται σε κάτι πανεύκολο:

$$
U(n) = U(n/2) + 1
$$

Εφαρμόζοντας την ίδια ταυτότητα ξανά και ξανά, οι όροι **τηλεσκοπούν**:

$$
\begin{aligned}
U(n) &= U(n/2) + 1 \\
&= U(n/4) + 1 + 1 \\
&\quad \vdots \\
&= U(n/n) + \underbrace{1 + 1 + \dots + 1}_{\log_2 n \text{ φορές}}
\end{aligned}
$$

Όταν φτάσουμε στο $U(1) = T(1)/1 = 0$, μένει $U(n) = \log_2 n$, άρα:

$$
T(n) = n \cdot U(n) = n \log_2 n
$$

<Callout type="key">
Η τεχνική «**διαίρεσε με $n$ και τηλεσκόπησε**» δουλεύει όποτε η σχέση είναι $T(n) = 2T(n/2) + n$. Είναι η πιο κομψή από τις τέσσερις — αξίζει να τη θυμάσαι απέξω.
</Callout>

## Απόδειξη #2 — επαγωγή στον διπλασιασμό

Επαγωγή πάνω στο $n$ (πάλι με $n = 2^k$). Θέλουμε να δείξουμε $T(n) = n \log n$.

**Βάση.** $T(1) = 0 = 1 \cdot \log 1$. ✓

**Επαγωγική υπόθεση.** Έστω ότι ισχύει $T(n) = n \log n$.

**Επαγωγικό βήμα.** Δείχνουμε ότι ισχύει και για τον **διπλάσιο** $2n$:

$$
\begin{aligned}
T(2n) &= 2\,T(n) + 2n \\
&= 2n \log n + 2n \\
&= 2n\,(\log n + 1) \\
&= 2n\,(\log n + \log 2) \\
&= 2n \log(2n)
\end{aligned}
$$

Χρησιμοποιήσαμε $\log 2 = 1$ (με $\log = \log_2$) και την ταυτότητα $\log a + \log b = \log(ab)$. $\blacksquare$

## Απόδειξη #3 — πολλαπλασιασμός γραμμών

Πιο γενική εκδοχή του τηλεσκοπίου, που δουλεύει για **οποιαδήποτε** σταθερά $c > 0$: αν $T(n) \le 2T(n/2) + c\,n$, τότε $T(n) \le c\,n \log n$.

**Πώς.** Γράφουμε τη σχέση επανειλημμένα, **πολλαπλασιάζοντας κάθε γραμμή με $2^{k-1}$**, ώστε οι μεσαίοι όροι να ταιριάξουν:

$$
\begin{aligned}
T(n) &\le 2\,T(n/2) + cn \\
2\,T(n/2) &\le 4\,T(n/4) + cn \\
4\,T(n/4) &\le 8\,T(n/8) + cn \\
&\quad \vdots \\
2^{k-1} T(n/2^{k-1}) &\le 2^{k} T(n/2^{k}) + cn
\end{aligned}
$$

**Προσθέτουμε** όλες τις γραμμές. Κάθε όρος της μορφής $2^{j} T(n/2^{j})$ εμφανίζεται μία φορά αριστερά και μία δεξιά — **τηλεσκοπεί** — και μένει:

$$
T(n) \le 2^{k} T(n/2^{k}) + cn \cdot k
$$

Για $n = 2^k$ (άρα $k = \log_2 n$) και $T(1) = 0$:

$$
T(n) \le 0 + cn \log n = cn \log n
$$

Άρα $T(n) = O(n \log n)$. $\blacksquare$

## Απόδειξη #4 — η γενική περίπτωση (με οροφές)

Οι τρεις πρώτες αποδείξεις υπέθεσαν $n = 2^k$. Τι γίνεται αν το $n$ είναι **οποιοσδήποτε** αριθμός (π.χ. 100, 1000, 13.547);

**Θεώρημα.** Για τη σχέση
$$
T(n) = \begin{cases} 0 & n = 1 \\ T(\lfloor n/2 \rfloor) + T(\lceil n/2 \rceil) + n & n > 1 \end{cases}
$$
ισχύει $T(n) \le n \lceil \log n \rceil$.

**Απόδειξη με ισχυρή επαγωγή στο $n$.**

**Βάση** ($n = 1$): $T(1) = 0 \le 1 \cdot \lceil \log 1 \rceil = 0$. ✓

**Επαγωγική υπόθεση.** Η σχέση ισχύει για κάθε $k = 1, 2, \dots, n-1$.

**Επαγωγικό βήμα.** Θέτουμε $n_1 = \lfloor n/2 \rfloor$ και $n_2 = \lceil n/2 \rceil$. Ισχύει $n_1 + n_2 = n$ και $n_1 \le n_2$.

$$
\begin{aligned}
T(n) &= T(n_1) + T(n_2) + n \\
&\le n_1 \lceil \log n_1 \rceil + n_2 \lceil \log n_2 \rceil + n & \text{(επαγ. υπόθεση)} \\
&\le n_1 \lceil \log n_2 \rceil + n_2 \lceil \log n_2 \rceil + n & (n_1 \le n_2) \\
&= n \lceil \log n_2 \rceil + n \\
&\le n (\lceil \log n \rceil - 1) + n & (\star) \\
&= n \lceil \log n \rceil
\end{aligned}
$$

**Δικαιολόγηση του $(\star)$.** Από τον ορισμό της οροφής, $n \le 2^{\lceil \log n \rceil}$, άρα:

$$
n_2 = \lceil n/2 \rceil \le \frac{2^{\lceil \log n \rceil}}{2} = 2^{\lceil \log n \rceil - 1}
$$

Παίρνοντας λογάριθμο, $\log n_2 \le \lceil \log n \rceil - 1$· και επειδή το δεξί μέλος είναι ακέραιος, ισχύει και με οροφή: $\lceil \log n_2 \rceil \le \lceil \log n \rceil - 1$. $\blacksquare$

<Callout type="warning">
**Γιατί χρειαζόμαστε αυτή τη «βαρετή» απόδειξη;** Γιατί μόνο αυτή δίνει εγγύηση για **κάθε** $n$, όχι μόνο για δυνάμεις του 2. Στην εξέταση, αν δεν ζητείται ρητά η γενική περίπτωση, χρησιμοποίησε την απλούστερη απόδειξη ($n = 2^k$) — είναι αποδεκτή για άνω/κάτω φράγμα.
</Callout>

<ExamProblem
  relatedExerciseId="pt1-th1-q5"
  pattern={
    <>
      <strong>Αν δεις την αναδρομή <InlineMath>{'T(n) = 2T(n/2) + n'}</InlineMath> οπουδήποτε (Σ/Λ, «κύκλωσε ποια ισχύουν», «βρες την πολυπλοκότητα»)</strong> — είναι ακριβώς η αναδρομή που μόλις λύσαμε με 4 τρόπους: η απάντηση είναι <em>πάντα</em> <InlineMath>{'\\Theta(n \\log n)'}</InlineMath>. Master Theorem με <InlineMath>{'a = 2,\\ b = 2,\\ d = 1'}</InlineMath> δίνει <InlineMath>{'d = \\log_b a'}</InlineMath> → περίπτωση 2 σε δύο γραμμές. Στις «κύκλωσε» ερωτήσεις: ναι στα φράγματα που περιέχουν τη σωστή τάξη (<InlineMath>{'O(n \\log n)'}</InlineMath> ✓, <InlineMath>{'o(n^3)'}</InlineMath> ✓), όχι στα στενότερα (<InlineMath>{'o(n)'}</InlineMath> ✗, <InlineMath>{'O(n)'}</InlineMath> ✗).
    </>
  }
/>

## Γιατί «στη μέση»; Και τι μετράει πραγματικά για την πολυπλοκότητα;

Τώρα μπορούμε να απαντήσουμε στην ερώτηση που αφήσαμε ανοιχτή — και μάλιστα να την επεκτείνουμε. Δύο διαφορετικά «λάθος» διαχωρίσματα: 

- **Αναλογικός αλλά άνισος**, π.χ. 60/40 ή 90/10. Το ένα κομμάτι είναι μεγαλύτερο, αλλά είναι πάντα **σταθερό ποσοστό** του γονικού.
- **Σταθερό κομμάτι**, π.χ. «$n-2$ και $2$». Το μικρό κομμάτι έχει **σταθερό μέγεθος**, ανεξάρτητο από το $n$.

Η διαίσθηση που μετράει είναι **το βάθος του δέντρου αναδρομής** — γιατί η δουλειά ανά επίπεδο μένει $\sim n$ έτσι κι αλλιώς (τα κομμάτια καλύπτουν όλη την είσοδο), οπότε το συνολικό κόστος είναι «βάθος × $n$». Δοκίμασε και τα δύο και πρόσεξε το σχήμα του δέντρου:

<SplitRatioExplorer />

Ο σταθερός διαχωρισμός γεννά μια **καμπύλη** — ένα μακρόστενο δέντρο όπου κάθε επίπεδο μειώνεται μόνο κατά $c$ — άρα χρειάζεται $n/c$ επίπεδα. Πολλαπλασίασε με τα $n$ ανά επίπεδο και βγαίνει $\Theta(n^2)$. Είναι ακριβώς το ίδιο πρόβλημα με την bubble sort, χωρίς καμιά βελτίωση.

<Callout type="key">
**Ο γενικός κανόνας.** Αρκεί ο διαχωρισμός να είναι **αναλογικός** — κάθε υποπρόβλημα να είναι **σταθερό κλάσμα** του γονικού (1/2, 1/3, 9/10· ό,τι θες, αρκεί να μην εξαρτάται από το $n$). Τότε το βάθος μένει $O(\log n)$ και η συνολική δουλειά $O(n \log n)$. Αυτό που **σκοτώνει** την πολυπλοκότητα είναι ο σταθερός διαχωρισμός — γιατί το βάθος γίνεται γραμμικό.
</Callout>

Με μαθηματικά: για διαχωρισμό «$n-2$ και $2$» η αναδρομή γίνεται $T(n) = T(n-2) + \Theta(n)$, και ξεδιπλώνοντας:
$$T(n) = \Theta(n) + \Theta(n-2) + \Theta(n-4) + \dots = \Theta(n^2)$$
Για διαχωρισμό $1/3 - 2/3$ (που είναι **αναλογικός**, απλώς όχι ισόρροπος) η αναδρομή είναι $T(n) = T(n/3) + T(2n/3) + cn$. Το βαθύτερο μονοπάτι ακολουθεί το μεγάλο κομμάτι: $n \to \tfrac{2}{3}n \to (\tfrac{2}{3})^2 n \to \dots$, φτάνει στο 1 σε $\log_{3/2} n$ βήματα — **ακόμη λογαριθμικό**. Κάθε επίπεδο πάλι $cn$ → $T(n) = O(n \log n)$, **ίδια τάξη με την ισόρροπη mergesort**.

## Όταν το D&C ΔΕΝ είναι γρήγορο: ο πύργος του Hanoi

Μέχρι τώρα το D&C φαίνεται μαγικό. Ένα διάσημο παράδειγμα δείχνει ότι **δεν είναι** — το σχήμα από μόνο του δεν εγγυάται τίποτα.

> **Πρόβλημα.** Υπάρχουν 3 στύλοι (A, B, C) και $n$ δίσκοι στοιβαγμένοι στον στύλο **A** σε κωνική μορφή (μεγάλος κάτω, μικρός πάνω). Θέλουμε να τους μεταφέρουμε όλους στον **C**, με κανόνες: (1) μετακινούμε **έναν δίσκο** κάθε φορά, (2) παίρνουμε πάντα τον **κορυφαίο** δίσκο ενός στύλου, (3) **ποτέ** δεν βάζουμε δίσκο πάνω σε μικρότερό του.

Το D&C το ξεκλειδώνει σε τρεις γραμμές. Παρατήρησε: για να μετακινήσεις τον **μεγαλύτερο** δίσκο, πρέπει πρώτα να έχεις «αδειάσει» τους $n-1$ από πάνω του στον βοηθητικό στύλο.

<Algorithm
  name="Πύργος του Hanoi (λύση D&C)"
  idea="Μετάφερε τους n−1 πάνω δίσκους στον βοηθητικό στύλο, μετά τον μεγάλο δίσκο στον στόχο, μετά πάλι τους n−1 από πάνω του."
  complexity="2ⁿ − 1 κινήσεις"
  io={{
    input: 'n δίσκοι στον στύλο «από»',
    output: 'και οι n δίσκοι στον στύλο «προς», με τη σωστή σειρά',
  }}
>

**Με λόγια.** Η βάση: για $n = 0$ δεν υπάρχει τίποτα να γίνει. Για $n$ δίσκους, υποθέτουμε (επαγωγική υπόθεση) ότι ξέρουμε να λύνουμε για $n-1$, και χτίζουμε τη λύση σε τρία βήματα: μετακινούμε αναδρομικά τους $n-1$ πάνω δίσκους στον **βοηθητικό** στύλο· μετακινούμε τον μεγαλύτερο δίσκο κατευθείαν στον στόχο· μετακινούμε αναδρομικά πάλι τους $n-1$ δίσκους πάνω του.

<Pseudocode
  code={`hanoi(n, από, προς, βοηθητικός):
  αν n = 0:  επίστρεψε
  hanoi(n − 1, από, βοηθητικός, προς)   // άδειασε τους n−1 στον βοηθητικό
  μετακίνησε 1 δίσκο: από → προς          // ο μεγαλύτερος δίσκος
  hanoi(n − 1, βοηθητικός, προς, από)   // ξαναβάλ' τους από πάνω`}
/>

</Algorithm>

Δες το να δουλεύει — και κυρίως πέρασε στην καρτέλα «Δες την έκρηξη» για να καταλάβεις γιατί αυτή η όμορφη D&C σχεδίαση γεννά εκθετικό κόστος:

<HanoiAnimator />

**Πολυπλοκότητα.** Έστω $T(n)$ ο αριθμός κινήσεων. Οι δύο αναδρομικές κλήσεις κοστίζουν $T(n-1)$ η καθεμία, συν 1 κίνηση για τον μεγάλο δίσκο:

$$
T(n) = 2\,T(n-1) + 1, \qquad T(1) = 1
$$

Λύνοντας (ξεδιπλώνοντας τη σχέση): $T(n) = 2^n - 1$. *(Έλεγχος: $T(1)=1$, $T(2)=3$, $T(3)=7=2^3-1$. ✓)*

<Callout type="warning">
**Η πικρή σύγκριση με τη mergesort.** Παρότι **και οι δύο** χρησιμοποιούν το σχήμα D&C, η mergesort είναι $O(n \log n)$ — *καλή* — ενώ ο Hanoi είναι $2^n - 1$ — *εκθετική, κακή*. Η διαφορά φαίνεται καθαρά στο δέντρο αναδρομής που μόλις είδες: στη mergesort, η συνολική δουλειά **ανά επίπεδο** μένει $n$ (γιατί τα κομμάτια μικραίνουν στον ίδιο ρυθμό που πληθαίνουν)· στον Hanoi, οι αναδρομικές κλήσεις πληθαίνουν αλλά **δεν μικραίνουν αναλογικά** — κάθε επίπεδο διπλασιάζει τη δουλειά.

**Το δίδαγμα:** το σχήμα D&C δεν είναι από μόνο του εγγύηση ταχύτητας. Χρειάζεται και ο **συνδυασμός** να είναι φτηνός σε σχέση με το μέγεθος των υποπροβλημάτων.
</Callout>

<ExamProblem
  relatedExerciseId="pt4-th1-q4"
  pattern={
    <>
      <strong>Αν δεις <InlineMath>{'aT(n - c) + \\Theta(n)'}</InlineMath> για <InlineMath>{'a > 1'}</InlineMath> (π.χ. <InlineMath>{'2T(n-1) + \\Theta(n)'}</InlineMath>) και σου προτείνουν <em>πολυωνυμικό</em> φράγμα</strong> — είναι <strong>η ίδια ακριβώς παγίδα του Hanoi</strong>: η σωστή απάντηση είναι <em>εκθετική</em>, όχι πολυωνυμική. Το πρόβλημα μικραίνει κατά <strong>σταθερά</strong> (όχι στο μισό), οπότε το βάθος του δέντρου είναι <InlineMath>{'n'}</InlineMath> (όχι <InlineMath>{'\\log n'}</InlineMath>) και τα φύλλα <InlineMath>{'a^n'}</InlineMath>. Ξεδιπλώνοντας: <InlineMath>{'2T(n-1) + cn = 2^n\\,T(0) + (\\text{πολυωνυμικοί όροι}) = \\Theta(2^n)'}</InlineMath>. <em>Διαχωριστής μοτίβου</em>: «μικραίνει κατά <InlineMath>{'c'}</InlineMath>» → εκθετικό· «μικραίνει στο <InlineMath>{'n/c'}</InlineMath>» → πολυωνυμικό. Το <InlineMath>{'O(n^2)'}</InlineMath> εδώ είναι παγίδα γιατί ηχεί σαν την mergesort — αλλά η mergesort έχει <InlineMath>{'2T(n/2)'}</InlineMath>, όχι <InlineMath>{'2T(n-1)'}</InlineMath>.
    </>
  }
/>

<RecallCard
  algorithm="Mergesort"
  keywords={['διαίρει στη μέση', 'αναδρομή στα δύο μισά', 'γραμμική συγχώνευση', 'n δουλειά ανά επίπεδο', 'log n επίπεδα']}
  skeleton={[
    'Βάση: τμήμα με ≤ 1 στοιχείο → ήδη ταξινομημένο.',
    'Διαίρεσε τον πίνακα σε δύο ίσα μισά.',
    'Ταξινόμησε αναδρομικά καθένα από τα δύο μισά.',
    'Συγχώνευσε τα δύο ταξινομημένα μισά (γραμμικό κόστος O(n)).',
  ]}
  complexity="O(n log n)"
  trap="Δύο παγίδες. (1) Η συγχώνευση δεν είναι in-place — θέλει βοηθητικό πίνακα O(n). (2) Ο διαχωρισμός πρέπει να είναι αναλογικός: «n−2 και 2» εκφυλίζει τον αλγόριθμο σε Θ(n²)."
/>

## Το Master Theorem

Παρατήρησε ένα μοτίβο: αναδρομικές σχέσεις της μορφής $T(n) = a\,T(n/b) + f(n)$ προκύπτουν **συνέχεια** στους αλγορίθμους D&C. Αντί να τις λύνουμε από την αρχή κάθε φορά, υπάρχει ένα **έτοιμο θεώρημα** που τις λύνει.

<Callout type="key">
**Master Theorem (Θεώρημα Κυρίαρχου Όρου).** Έστω σταθερές $a \ge 1$, $b > 1$, $d \ge 0$ και $T(n)$ ορισμένη ως:

$$
T(n) = a \cdot T(n/b) + O(n^d)
$$

Τότε:

$$
T(n) = \begin{cases}
O(n^d) & \text{αν } d > \log_b a \\[4pt]
O(n^d \log n) & \text{αν } d = \log_b a \\[4pt]
O(n^{\log_b a}) & \text{αν } d < \log_b a
\end{cases}
$$
</Callout>

Τα τρία σύμβολα: $a$ = πόσα υποπροβλήματα φτιάχνεις, $b$ = πόσες φορές μικραίνει το καθένα, $d$ = ο εκθέτης του κόστους του **συνδυαστικού βήματος** $f(n) = O(n^d)$. Όλη η ουσία είναι μια **σύγκριση**: ο εκθέτης $d$ (κόστος συνδυασμού) εναντίον του $\log_b a$ (ρυθμός που πληθαίνει το δέντρο). **Ό,τι είναι μεγαλύτερο, κυριαρχεί.**

<svg viewBox="0 0 760 280" className="my-4 w-full max-w-3xl" role="img" aria-label="Δέντρο απόφασης για τις τρεις περιπτώσεις του Master Theorem">
  <style>{`
    .mt-root { fill: rgb(var(--accent) / 0.18); stroke: rgb(var(--accent)); stroke-width: 2; }
    .mt-node { fill: rgb(var(--bg-elevated)); stroke: rgb(var(--accent)); stroke-width: 1.5; }
    .mt-result { fill: rgb(var(--accent) / 0.08); stroke: rgb(var(--accent)); stroke-width: 1.5; stroke-dasharray: 4 3; }
    .mt-edge { stroke: rgb(var(--border-strong)); stroke-width: 1.5; fill: none; }
    .mt-edge-l { font-family: ui-sans-serif, sans-serif; font-size: 11px; font-weight: 600; fill: rgb(var(--accent)); }
    .mt-title { font-family: ui-sans-serif, sans-serif; font-size: 13px; font-weight: 700; fill: rgb(var(--fg)); }
    .mt-math { font-family: ui-monospace, monospace; font-size: 12px; fill: rgb(var(--fg)); }
    .mt-result-text { font-family: ui-monospace, monospace; font-size: 13px; font-weight: 700; fill: rgb(var(--accent)); }
  `}</style>
  <g>
    <rect x="290" y="14" width="180" height="50" rx="8" className="mt-root" />
    <text x="380" y="36" textAnchor="middle" className="mt-title">Σύγκρινε d με logₐ a</text>
    <text x="380" y="55" textAnchor="middle" className="mt-math">r = a / b^d</text>
  </g>
  <line x1="320" y1="64" x2="120" y2="110" className="mt-edge" />
  <line x1="380" y1="64" x2="380" y2="110" className="mt-edge" />
  <line x1="440" y1="64" x2="640" y2="110" className="mt-edge" />
  <text x="195" y="90" className="mt-edge-l">d &gt; log_b a (r &lt; 1)</text>
  <text x="396" y="90" className="mt-edge-l">d = log_b a (r = 1)</text>
  <text x="565" y="90" className="mt-edge-l">d &lt; log_b a (r &gt; 1)</text>
  <g>
    <rect x="40" y="110" width="160" height="60" rx="8" className="mt-node" />
    <text x="120" y="135" textAnchor="middle" className="mt-title">Περίπτωση 1</text>
    <text x="120" y="155" textAnchor="middle" className="mt-math">«η ρίζα κυριαρχεί»</text>
  </g>
  <g>
    <rect x="300" y="110" width="160" height="60" rx="8" className="mt-node" />
    <text x="380" y="135" textAnchor="middle" className="mt-title">Περίπτωση 2</text>
    <text x="380" y="155" textAnchor="middle" className="mt-math">«ισορροπία»</text>
  </g>
  <g>
    <rect x="560" y="110" width="160" height="60" rx="8" className="mt-node" />
    <text x="640" y="135" textAnchor="middle" className="mt-title">Περίπτωση 3</text>
    <text x="640" y="155" textAnchor="middle" className="mt-math">«τα φύλλα κυριαρχούν»</text>
  </g>
  <line x1="120" y1="170" x2="120" y2="200" className="mt-edge" />
  <line x1="380" y1="170" x2="380" y2="200" className="mt-edge" />
  <line x1="640" y1="170" x2="640" y2="200" className="mt-edge" />
  <g>
    <rect x="40" y="200" width="160" height="60" rx="8" className="mt-result" />
    <text x="120" y="232" textAnchor="middle" className="mt-result-text">T(n) = O(n^d)</text>
  </g>
  <g>
    <rect x="300" y="200" width="160" height="60" rx="8" className="mt-result" />
    <text x="380" y="232" textAnchor="middle" className="mt-result-text">O(n^d log n)</text>
  </g>
  <g>
    <rect x="560" y="200" width="160" height="60" rx="8" className="mt-result" />
    <text x="640" y="232" textAnchor="middle" className="mt-result-text">O(n^(log_b a))</text>
  </g>
</svg>

### Διαισθητική απόδειξη μέσω δέντρου αναδρομής

Από πού βγαίνουν οι τρεις περιπτώσεις; Ξαναγυρνάμε στο δέντρο αναδρομής. Στο **επίπεδο $k$**:

- υπάρχουν $a^k$ υποπροβλήματα,
- καθένα έχει μέγεθος $n/b^k$,
- καθένα απαιτεί $O\big((n/b^k)^d\big)$ συνδυαστική δουλειά.

Άρα η **συνολική δουλειά στο επίπεδο $k$** είναι:

$$
a^k \cdot O\!\left(\left(\frac{n}{b^k}\right)^{d}\right) = O(n^d) \cdot \left(\frac{a}{b^d}\right)^{k}
$$

Όλο εξαρτάται από τον **λόγο** $r = a / b^d$ — πόσο αλλάζει η δουλειά από επίπεδο σε επίπεδο. Αθροίζοντας για όλα τα $\log_b n$ επίπεδα:

$$
T(n) = O(n^d) \cdot \sum_{k=0}^{\log_b n} r^k
$$

Αυτό είναι ένα **γεωμετρικό άθροισμα**, και η συμπεριφορά του εξαρτάται απόλυτα από το $r$:

- $r < 1$ (ισοδύναμα $d > \log_b a$): η δουλειά **φθίνει**· το άθροισμα συγκλίνει σε σταθερά → η **ρίζα** κυριαρχεί → $O(n^d)$.
- $r = 1$ (ισοδύναμα $d = \log_b a$): κάθε επίπεδο **ίδιο**· το άθροισμα είναι $\log_b n + 1$ → $O(n^d \log n)$.
- $r > 1$ (ισοδύναμα $d < \log_b a$): η δουλειά **αυξάνει**· το άθροισμα κυριαρχείται από τον τελευταίο όρο → τα **φύλλα** κυριαρχούν → $O(n^{\log_b a})$.

*(Για την τρίτη περίπτωση: $r^{\log_b n} = (a/b^d)^{\log_b n} = n^{\log_b a}/n^d$, και το $O(n^d)$ μπροστά απλοποιεί τον παρονομαστή.)*

Δοκίμασέ το μόνος σου. Διάλεξε $a, b, d$ — ή πάτησε ένα έτοιμο παράδειγμα — και δες ποια περίπτωση προκύπτει και **γιατί** (η ράβδος δείχνει τη δουλειά ανά επίπεδο):

<RecurrenceClassifier />

## Εφαρμογές του Master Theorem

Τέσσερις γνωστές αναδρομές, λυμένες σε μία γραμμή η καθεμία:

| Αλγόριθμος | Αναδρομή | $a, b, d$ | $\log_b a$ vs $d$ | Αποτέλεσμα |
|---|---|---|---|---|
| **Mergesort** | $2T(n/2) + n$ | $2, 2, 1$ | $1 = 1$ → Περ. 2 | $O(n \log n)$ |
| **Δυαδική αναζήτηση** | $T(n/2) + 1$ | $1, 2, 0$ | $0 = 0$ → Περ. 2 | $O(\log n)$ |
| **Karatsuba** ([L04](/lectures/L04-divide-and-conquer-ii)) | $3T(n/2) + n$ | $3, 2, 1$ | $1.585 > 1$ → Περ. 3 | $O(n^{1.585})$ |
| **Γραμμική επιλογή** | $T(n/2) + n$ | $1, 2, 1$ | $0 < 1$ → Περ. 1 | $O(n)$ |

Δύο πράγματα αξίζουν προσοχή. Το Master Theorem δίνει για τη mergesort το ίδιο αποτέλεσμα με τις τέσσερις αποδείξεις παραπάνω — σε **μία γραμμή**. Και για τον Karatsuba προβλέπει $O(n^{1.585})$, που είναι **καλύτερο** από το αφελές $\Theta(n^2)$ του πολλαπλασιασμού — γι' αυτό το κόλπο αξίζει, και θα το δούμε αναλυτικά στο [L04](/lectures/L04-divide-and-conquer-ii).

<ThinkingPattern
  title="Μπροστά σε μια αναδρομική σχέση T(n) = a·T(n/b) + f(n)"
  signals={['βρες την πολυπλοκότητα αναδρομικού αλγορίθμου', 'λύσε την αναδρομή', 'T(n) = a T(n/b) + …']}
>
Όταν σου δίνουν αναδρομή και ζητούν την πολυπλοκότητα:

1. **Έλεγξε αν εφαρμόζεται το Master Theorem.** Χρειάζεσαι ίδιο $b$ σε όλες τις κλήσεις και $f(n)$ της μορφής $n^d$.
2. **Αν ναι:** υπολόγισε $\log_b a$, σύγκρινέ το με το $d$, διάλεξε περίπτωση. Τρεις γραμμές, τελείωσες.
3. **Αν όχι** (άνισος διαχωρισμός, μη πολυωνυμικό $f$): γύρνα στο **δέντρο αναδρομής** ή στο **τηλεσκόπιο** — τις τεχνικές των αποδείξεων #1–#4.

Στην εξέταση, το Master Theorem είναι ο **γρηγορότερος** δρόμος όταν εφαρμόζεται — αλλά πρέπει να ξέρεις και τον δεύτερο δρόμο για όταν δεν εφαρμόζεται.
</ThinkingPattern>

<ExamProblem
  relatedExerciseId="pt4-th3"
  pattern={
    <>
      <strong>Σχεδίαση D&amp;C — μονοτονική είσοδος (πίνακας, συμβολοσειρά) με κρυφό «σύνορο», ζητείται χρόνος <InlineMath>{'O(\\log)'}</InlineMath></strong> — η ιδέα είναι <em>δυαδική αναζήτηση όχι για τιμή αλλά για όριο</em>: στη μεσαία θέση <InlineMath>{'q'}</InlineMath>, διάβασε το στοιχείο και αποφάσισε σε ποιο μισό κάθεται το σύνορο — μετά αναδρομή σ' εκείνο. Η αναδρομή που προκύπτει είναι <InlineMath>{'T(k) = T(k/2) + O(1)'}</InlineMath>, <em>ίδια ακριβώς</em> με τη γραμμή «Δυαδική αναζήτηση» του πίνακα παραπάνω: <InlineMath>{'a = 1,\\ b = 2,\\ d = 0'}</InlineMath>, <InlineMath>{'d = \\log_b a'}</InlineMath> → Master Theorem περίπτωση 2 → <InlineMath>{'\\Theta(\\log k)'}</InlineMath>. Επειδή η εκφώνηση ζητάει <em>φυσική γλώσσα</em>, αρκεί να γράψεις «κάθε βήμα κόβει το πρόβλημα στη μέση κοιτώντας το μεσαίο σύμβολο» χωρίς ψευδοκώδικα γραμμή-γραμμή.
    </>
  }
/>

## Πότε ΔΕΝ εφαρμόζεται το Master Theorem

Το θεώρημα είναι ισχυρό αλλά **όχι παντοδύναμο**. Δεν καλύπτει:

1. **Άνισο διαχωρισμό** — π.χ. $T(n) = T(n/3) + T(2n/3) + n$. Τα κομμάτια δεν έχουν κοινό $b$. → Λύσε με δέντρο αναδρομής (το κάναμε ήδη παραπάνω: $O(n \log n)$).
2. **Μη πολυωνυμικό $f(n)$** — π.χ. $T(n) = 2T(n/2) + n \log n$. Το $n \log n$ δεν είναι της μορφής $n^d$. → Χρειάζεται γενικευμένη εκδοχή.
3. **$a < 1$ ή $b \le 1$** — δεν έχει νόημα στο μοντέλο (δεν υπάρχει «μισό υποπρόβλημα», το πρόβλημα πρέπει να μικραίνει).

Σε αυτές τις περιπτώσεις **επιστρέφουμε στις τεχνικές #1–#4** — γι' αυτό αξίζει να τις ξέρεις, παρά την ύπαρξη του Master Theorem.

## Είναι τελικά η mergesort η καλύτερη δυνατή;

Κλείνουμε με μια ερώτηση που ανοίξαμε στο [L02](/lectures/L02-asymptotic-analysis): η mergesort τρέχει σε $O(n \log n)$ — μπορεί κανείς να κάνει καλύτερα;

Για ταξινόμηση που χρησιμοποιεί **μόνο συγκρίσεις** στοιχείων, η απάντηση είναι **όχι**:

> **Θεώρημα (κάτω φράγμα).** Κάθε αλγόριθμος ταξινόμησης που βασίζεται μόνο σε συγκρίσεις απαιτεί $\Omega(n \log n)$ συγκρίσεις στη χείριστη περίπτωση.

Η απόδειξη είναι μια γεωμετρική παρατήρηση. Φαντάσου τον αλγόριθμό σου σαν ένα **δέντρο απόφασης**: η ρίζα είναι η πρώτη σύγκριση που θα κάνει, οι δύο ακμές είναι «ναι»/«όχι», και κάθε επόμενος εσωτερικός κόμβος είναι η επόμενη σύγκριση. Το **βάθος** της εκτέλεσης = ο αριθμός των συγκρίσεων που θα κάνει. Το **ύψος** του δέντρου = η χείριστη περίπτωση.

Δες το πραγματικά να συμβαίνει — διάλεξε μια διάταξη και φωτίζεται το μονοπάτι που πρέπει να ακολουθήσει κάθε αλγόριθμος για να την εντοπίσει:

<DecisionTreeLowerBound />

Η ουσία σε δύο σειρές: $n!$ διαφορετικές διατάξεις πρέπει να καταλήγουν σε $n!$ διαφορετικά φύλλα· ένα δυαδικό δέντρο με $n!$ φύλλα έχει ύψος $\ge \lceil\log_2(n!)\rceil = \Theta(n \log n)$. Άρα η mergesort, που δίνει $O(n \log n)$ **άνω** φράγμα, ακουμπάει το κάτω φράγμα — είναι **ασυμπτωτικά βέλτιστη**.

<Callout type="intuition">
Ο μόνος τρόπος να σπάσεις το $\Omega(n \log n)$ είναι να **μη** βασιστείς αποκλειστικά σε συγκρίσεις — π.χ. counting sort / radix sort, που εκμεταλλεύονται τη δομή των δεδομένων (ακέραιοι σε γνωστό εύρος) και τρέχουν σε $O(n)$. Λειτουργούν όμως μόνο σε ειδικές περιπτώσεις· για ταξινόμηση γενικού σκοπού, το $n \log n$ της mergesort είναι το όριο.
</Callout>

<RecallCard
  algorithm="Master Theorem"
  keywords={['T(n) = a·T(n/b) + O(nᵈ)', 'σύγκρινε d με logᵦa', 'r = a/bᵈ', 'τρεις περιπτώσεις']}
  skeleton={[
    'Γράψε την αναδρομή στη μορφή a·T(n/b) + O(nᵈ).',
    'Υπολόγισε το logᵦa.',
    'Σύγκρινέ το με το d.',
    'd > logᵦa → O(nᵈ) · d = logᵦa → O(nᵈ log n) · d < logᵦa → O(n^logᵦa).',
  ]}
  complexity="3 περιπτώσεις"
  trap="Δεν εφαρμόζεται σε άνισο διαχωρισμό (π.χ. T(n/3)+T(2n/3)) ούτε όταν το f(n) δεν είναι πολυωνυμικό (π.χ. n log n). Εκεί γύρνα σε δέντρο αναδρομής ή τηλεσκόπιο."
/>

## Κλείδωσε τη γνώση

Πρώτα η σκελετική δομή της mergesort — βάλε τα βήματα στη σωστή σειρά:

<ReorderDrill
  prompt="Τα βήματα της mergesort, από την πρώτη ενέργεια στην τελευταία."
  steps={[
    { id: 'm1', text: 'Βάση: αν το τμήμα έχει ≤ 1 στοιχείο, επίστρεψέ το ως έχει — είναι ήδη ταξινομημένο.' },
    { id: 'm2', text: 'Διαίρεσε τον πίνακα σε δύο μισά, σπάζοντάς τον στη μέση.' },
    { id: 'm3', text: 'Κάλεσε αναδρομικά τη mergesort σε καθένα από τα δύο μισά.' },
    { id: 'm4', text: 'Συγχώνευσε τα δύο ταξινομημένα μισά σε έναν ταξινομημένο πίνακα.' },
  ]}
/>

Τώρα το Master Theorem — συμπλήρωσε τα κενά χωρίς να κοιτάς πίσω:

<ClozeDrill
  prompt="Πώς δουλεύει το Master Theorem, σε λέξεις."
  segments={[
    { text: 'Το Master Theorem λύνει αναδρομές της μορφής T(n) = a·T(n/b) + O(nᵈ). Συγκρίνουμε τον εκθέτη ' },
    { blank: 'd', hint: 'εκθέτης συνδυασμού' },
    { text: ' με το logᵦa. Αν το d είναι μεγαλύτερο, κυριαρχεί η ' },
    { blank: 'ρίζα', hint: 'πρώτο επίπεδο' },
    { text: ' του δέντρου αναδρομής. Αν είναι ίσα, κάθε επίπεδο κοστίζει το ίδιο κι εμφανίζεται ένας παράγοντας ' },
    { blank: 'log', hint: '… n' },
    { text: ' n. Αν το d είναι μικρότερο, κυριαρχούν τα ' },
    { blank: 'φύλλα' },
    { text: ' και η απάντηση γίνεται O(n^logᵦa).' },
  ]}
/>

<ExamRadar
  items={[
    {
      topic: 'Απόδειξη ότι T(n) = 2T(n/2) + n δίνει O(n log n) — με δέντρο αναδρομής ή τηλεσκόπιο',
      likelihood: 'high',
      relatedExerciseIds: ['front-set-3-ask8', 'front-set-4-ask2'],
    },
    {
      topic: 'Εφαρμογή Master Theorem: δοσμένη αναδρομή → ποια περίπτωση, ποιο αποτέλεσμα',
      likelihood: 'high',
      relatedExerciseIds: [
        'pt1-th1-q5',
        'pt2-th1-q3',
        'pt5-th2-b',
        'front-set-3-ask7',
      ],
    },
    {
      topic: 'Σχεδίαση αλγορίθμου «διαίρει και κυρίευε» για νέο πρόβλημα και ανάλυση της πολυπλοκότητάς του',
      likelihood: 'high',
      note: 'Η D&C είναι η πρώτη τεχνική σχεδίασης — εμφανίζεται ως «σχεδίασε αλγόριθμο που…».',
      relatedExerciseIds: ['pt1-th4', 'pt4-th3', 'front-set-4-ask7', 'front-set-5-ask1'],
    },
    {
      topic: 'Λύση αναδρομής όταν το Master Theorem δεν εφαρμόζεται (άνισος διαχωρισμός, μη πολυωνυμικό f)',
      likelihood: 'medium',
      relatedExerciseIds: ['front-set-3-ask4', 'front-set-3-ask9', 'front-set-4-ask4', 'front-set-4-ask10'],
    },
    {
      topic: 'Ανάλυση αναδρομής τύπου Hanoi T(n) = 2T(n−1) + 1 → εκθετική',
      likelihood: 'medium',
      relatedExerciseIds: ['pt4-th1-q4', 'front-set-3-ask4'],
    },
    {
      topic: 'Αναδρομές με ρίζα — αντικατάσταση n = 2ᵐ',
      likelihood: 'high',
      note: 'Το «αποτύπωμα» είναι ένα διπλό log στην απάντηση (log log n).',
      relatedExerciseIds: ['pt1-th1-q4', 'pt2-th1-q4', 'front-set-3-ask10', 'front-set-4-ask1'],
    },
    {
      topic: 'Ομογενείς γραμμικές αναδρομές — χαρακτηριστική εξίσωση (Fibonacci, διπλή ρίζα)',
      likelihood: 'medium',
      relatedExerciseIds: ['front-set-3-ask1', 'front-set-3-ask2'],
    },
    {
      topic: 'Μέθοδος αντικατάστασης — εικασία + ενίσχυση εικασίας όταν χρειάζεται',
      likelihood: 'medium',
      relatedExerciseIds: ['front-set-4-ask2', 'front-set-4-ask3'],
    },
  ]}
/>

<Recap>
**Τι κρατάμε από αυτή τη διάλεξη**

1. **D&C ως σχήμα:** διαίρεσε σε ξένα υποπροβλήματα → κυρίευσε αναδρομικά → συνδύασε. Είναι επαγωγή μεταμφιεσμένη, και η ορθότητα αποδεικνύεται με επαγωγή.
2. **Mergesort:** σπάσε στη μέση, ταξινόμησε αναδρομικά, συγχώνευσε με γραμμικό κόστος → $O(n \log n)$. Κάθε επίπεδο του δέντρου κοστίζει $n$, και τα επίπεδα είναι $\log n$.
3. **Τέσσερις αποδείξεις** του $O(n \log n)$ — μάθε τουλάχιστον δύο. Το τηλεσκόπιο (#1, #3) είναι το πιο ευέλικτο.
4. **Γιατί «στη μέση»:** μόνο ο **αναλογικός** διαχωρισμός δίνει βάθος $\log n$. Το «$n-2$ και $2$» εκφυλίζει τον αλγόριθμο σε $\Theta(n^2)$.
5. **Πύργος Hanoi:** D&C με **εκθετικό** κόστος ($2^n - 1$). Το σχήμα από μόνο του δεν εγγυάται γρήγορο αλγόριθμο — μετράει το κόστος του συνδυασμού.
6. **Master Theorem:** για $T(n) = a\,T(n/b) + O(n^d)$, σύγκρινε $d$ με $\log_b a$ → τρεις περιπτώσεις. Γρήγορο, όταν εφαρμόζεται.
7. Η **mergesort είναι ασυμπτωτικά βέλτιστη** για ταξινόμηση με συγκρίσεις — το κάτω φράγμα $\Omega(n \log n)$ ακουμπάει το άνω φράγμα της.
</Recap>

<NextUp current="lectures/L03-divide-and-conquer-i" />

<LectureExercises lectureSlug="lectures/L03-divide-and-conquer-i" />
