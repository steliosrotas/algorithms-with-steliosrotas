export const metadata = {
  title: 'L14 · Δυναμικός Προγραμματισμός I — Memoization & Σταθμισμένος Χρονοπρογραμματισμός',
  description:
    'Η ιδέα του δυναμικού προγραμματισμού: επικαλυπτόμενα υποπροβλήματα και memoization. Fibonacci, σταθμισμένος χρονοπρογραμματισμός διαστημάτων, και η συνταγή 4 βημάτων του DP.',
}

export const frontmatter = {
  title: 'L14 · Δυναμικός Προγραμματισμός I — Memoization & Σταθμισμένος Χρονοπρογραμματισμός',
  slug: 'lectures/L14-dp-i',
  order: 14,
  prerequisites: ['lectures/L11-greedy-i'],
  examWeight: 9,
  estimatedReadTime: 44,
  lastUpdated: '2026-05-22',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Dynamic programming</span>
  <span>·</span>
  <span>Διάλεξη 14</span>
  <span>·</span>
  <span>~44 min</span>
</div>

# L14 · Δυναμικός Προγραμματισμός I — Memoization &amp; Σταθμισμένος Χρονοπρογραμματισμός

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="lectures/L14-dp-i" />
  <Bookmark id="lectures/L14-dp-i" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc
  sources={{ pdf: 'L14 - Δυναμικός Προγραμματισμός Ι.pdf' }}
  note="68 διαφάνειες — η εισαγωγή στον δυναμικό προγραμματισμό, το πιο εξεταζόμενο κεφάλαιο της ύλης."
/>

## Τι θα δούμε

Ξεκινά το τελευταίο μεγάλο κεφάλαιο: ο **δυναμικός προγραμματισμός** (dynamic programming, DP). Είναι το πιο εξεταζόμενο κομμάτι της ύλης — και αυτό που οι φοιτητές βρίσκουν πιο «μαγικό». Στόχος αυτής της διάλεξης: να πάψει να είναι μαγικό.

Θα δούμε την ιδέα μέσα από το απλούστερο παράδειγμα (**Fibonacci**), μετά το πρώτο «πραγματικό» πρόβλημα (**σταθμισμένος χρονοπρογραμματισμός διαστημάτων**), και θα κωδικοποιήσουμε μια **συνταγή 4 βημάτων** που εφαρμόζεται σε **κάθε** DP πρόβλημα.

## Τα τρία αλγοριθμικά μοντέλα

<Callout type="key">
- **Απληστία** — χτίσε τη λύση σταδιακά, βελτιστοποιώντας μυωπικά ένα τοπικό κριτήριο. *([L11](/lectures/L11-greedy-i)–[L13](/lectures/L13-greedy-iii))*
- **Διαίρει και κυρίευε** — διάσπασε σε **ανεξάρτητα** υποπροβλήματα, λύσε το καθένα ξεχωριστά, συνδύασε. *([L03](/lectures/L03-divide-and-conquer-i)–[L05](/lectures/L05-divide-and-conquer-iii))*
- **Δυναμικός προγραμματισμός** — διάσπασε σε μια σειρά από **επικαλυπτόμενα** υποπροβλήματα, και δόμησε σωστές λύσεις για όλο και μεγαλύτερα.
</Callout>

<Callout type="intuition">
**Η κρίσιμη λέξη είναι «επικαλυπτόμενα».** Στο διαίρει-και-κυρίευε τα δύο μισά ενός πίνακα είναι **ξένα** — δεν μοιράζονται τίποτα. Στον δυναμικό προγραμματισμό τα υποπροβλήματα **ξανασυναντιούνται**: το ίδιο υποπρόβλημα χρειάζεται πολλές φορές. Αν το λύνεις από την αρχή κάθε φορά, πληρώνεις εκθετικά. Αν το λύνεις **μία φορά και το θυμάσαι**, πληρώνεις γραμμικά. Αυτή είναι όλη η ιδέα.
</Callout>

<Callout type="note">
**Από πού το όνομα;** Ο Richard Bellman θεμελίωσε τον δυναμικό προγραμματισμό τη δεκαετία του 1950. Το «programming» εδώ σημαίνει **σχεδιασμός** (όπως στο «linear programming»), όχι κώδικας. Ο Bellman διάλεξε επίτηδες ένα εντυπωσιακό όνομα — «είναι αδύνατο να χρησιμοποιήσεις τη λέξη *dynamic* με υποτιμητική έννοια», κάτι που ούτε ένα μέλος του Κογκρέσου δεν θα απέρριπτε ως χρηματοδότηση. Διάσημοι DP αλγόριθμοι: **Viterbi**, **unix diff**, **Smith-Waterman**, **Bellman-Ford**, **CKY**.
</Callout>

## Το παράδειγμα-εκκίνηση: Fibonacci

Η ακολουθία Fibonacci ορίζεται αναδρομικά:

$$F(n) = F(n-1) + F(n-2), \qquad F(0) = 0,\ F(1) = 1.$$

Η απευθείας μεταφορά σε κώδικα είναι κομψή — και **καταστροφική**:

<Pseudocode
  title="Αφελής Fibonacci — εκθετικός"
  defaultOpen
  code={`fib(n):
  αν n ≤ 1:  επίστρεψε n
  επίστρεψε fib(n−1) + fib(n−2)`}
  caption="Σωστό, αλλά η πολυπλοκότητα είναι ≈ O(2ⁿ) — εκθετική."
/>

### Γιατί είναι τόσο αργό; Επικάλυψη υποπροβλημάτων

Ο κώδικας είναι σωστός — γιατί όμως είναι τόσο αργός; Αντί να το συμπεράνουμε, ας **παρακολουθήσουμε** τι κάνει στ' αλήθεια η `fib`. Κάθε κλήση `fib(k)` γεννά δύο νέες, τις `fib(k−1)` και `fib(k−2)`· αυτές γεννούν άλλες δύο η καθεμιά, και ούτω καθεξής. Όλες μαζί σχηματίζουν ένα **δέντρο αναδρομής** — κι εκεί κρύβεται το πρόβλημα.

<RecursionExplosion />

Σύρε το `n` (κράτησέ το για τώρα στο **«Αφελής»**) και πρόσεξε δύο πράγματα:

- Το δέντρο σχεδόν **διπλασιάζεται** σε κάθε επιπλέον επίπεδο. Ο μετρητής κλήσεων δεν ανεβαίνει γραμμικά — **εκτοξεύεται**, περίπου σαν $2^n$. Ο πίνακας στο τέλος δείχνει πού φτάνει αυτό: το `fib(30)` θέλει πάνω από δύο εκατομμύρια κλήσεις.
- Πάτησε σε έναν κόμβο — π.χ. σε ένα `fib(2)`. Φωτίζονται **όλες** οι άλλες κλήσεις του ίδιου `fib(2)` στο δέντρο. Είναι πολλές· και η καθεμιά ξαναϋπολογίζει από την αρχή ακριβώς την ίδια τιμή.

Αυτό το δεύτερο είναι η ρίζα του κακού, και έχει όνομα: **επικάλυψη υποπροβλημάτων** (overlapping subproblems). Το ίδιο υποπρόβλημα — «πόσο κάνει το `fib(2)`;» — ξαναεμφανίζεται αμέτρητες φορές μέσα στην αναδρομή. Ο αφελής αλγόριθμος δεν είναι αργός επειδή έχει δύσκολη δουλειά· είναι αργός επειδή κάνει την **ίδια εύκολη δουλειά ξανά και ξανά**.

### Η λύση: memoization

Αφού το πρόβλημα είναι ότι υπολογίζουμε το ίδιο πράγμα πολλές φορές, η λύση γράφεται σχεδόν μόνη της: **υπολόγισέ το μία φορά, και κράτησέ το.**

<Callout type="key">
**Απομνημόνευση (memoization):** κάθε φορά που υπολογίζεις έναν όρο $F(k)$, **αποθήκευσέ τον** σε έναν πίνακα. Όταν τον ξαναζητήσει η αναδρομή, **επίστρεψε την αποθηκευμένη τιμή** αντί να ξαναξεκινήσεις τον υπολογισμό.
</Callout>

Γύρνα τώρα στο interactive παραπάνω και πάτα το κουμπί **«Memoized»**. Ίδιο `n`, ίδιο πρόβλημα — αλλά το δέντρο **καταρρέει**: κάθε `fib(k)` υπολογίζεται μία μόνο φορά, και κάθε επόμενη φορά που ζητείται επιστρέφεται **έτοιμο** από τον πίνακα (ο πράσινος κόμβος «cache hit», χωρίς υποδέντρο από κάτω). Ο μετρητής κλήσεων πέφτει από εκθετικός σε **γραμμικό**.

Στην πράξη το ίδιο αποτέλεσμα το πετυχαίνουμε ακόμα πιο καθαρά — χωρίς καθόλου αναδρομή. Γεμίζουμε έναν πίνακα **από κάτω προς τα πάνω** (bottom-up): πρώτα τις βασικές περιπτώσεις, και μετά κάθε επόμενο όρο από τους δύο προηγούμενους.

<Pseudocode
  title="Fibonacci με πίνακα — γραμμικός"
  defaultOpen
  code={`fib2(n):
  αν n ≤ 1:  επίστρεψε n
  A[0] ← 0 ;  A[1] ← 1
  για i = 2 έως n:
    A[i] ← A[i−1] + A[i−2]
  επίστρεψε A[n]`}
  caption="Κάθε F(k) υπολογίζεται ακριβώς μία φορά → O(n) αντί για O(2ⁿ)."
/>

Κάθε $F(k)$ υπολογίζεται **ακριβώς μία φορά**. Η πολυπλοκότητα πέφτει από εκθετική $O(2^n)$ σε **γραμμική $O(n)$** — αλλάζοντας μόνο *πού* αποθηκεύεις τα ενδιάμεσα αποτελέσματα.

<ExamProblem
  relatedExerciseId="pt7-th2"
  pattern={
    <>
      <strong>Αν η εκφώνηση δίνει αναδρομή <InlineMath>{'b_n = f(b_{n-1}, b_{n-2}, \\dots)'}</InlineMath> και έναν αλγόριθμο <code>RB(n)</code> που τη μιμείται απευθείας</strong> — δείξε ότι ο <code>RB</code> είναι εκθετικός με γεωμετρικό επιχείρημα στο δέντρο αναδρομής: <InlineMath>{'k'}</InlineMath> κλήσεις ανά κόμβο και βάθος <InlineMath>{'\\ge n / \\max d_i'}</InlineMath>, άρα <InlineMath>{'T(n) = \\Omega(k^{\\,n/\\max d_i})'}</InlineMath>. Με memoization τα *διαφορετικά* ορίσματα είναι μόνο <InlineMath>{'n'}</InlineMath> → <InlineMath>{'\\Theta(n)'}</InlineMath>.
    </>
  }
/>

## Σταθμισμένος χρονοπρογραμματισμός διαστημάτων

Τώρα το πρώτο πραγματικό πρόβλημα. Είναι το πρόβλημα του [L11](/lectures/L11-greedy-i) — **με μία προσθήκη**.

**Είσοδος:** $n$ αιτήματα. Το αίτημα $j$ ξεκινά τη στιγμή $s_j$, τελειώνει τη στιγμή $f_j$, και έχει **βαρύτητα** $v_j$. Δύο αιτήματα είναι **συμβατά** αν δεν επικαλύπτονται.

**Στόχος:** υποσύνολο $S$ ανά δύο συμβατών αιτημάτων με **μέγιστο άθροισμα βαρυτήτων** $\sum_{i \in S} v_i$.

### Γιατί δεν αρκεί ο άπληστος

Στο [L11](/lectures/L11-greedy-i), όταν **όλες οι βαρύτητες ήταν ίσες με $1$**, το άπληστο κριτήριο «μικρότερος χρόνος λήξης» έδινε αποδεδειγμένα βέλτιστη λύση. Το ένστικτο λέει πως κάποιο άπληστο κριτήριο θα δουλεύει και τώρα που οι εργασίες έχουν βαρύτητες. Δοκίμασε τα δύο πιο φυσικά — και δες τα να αποτυγχάνουν:

<GreedyFailsWeighted />

Στο πρώτο στιγμιότυπο, μια **φθηνή** εργασία που τυχαίνει να τελειώνει πρώτη μπλοκάρει μια εργασία που αξίζει $100$. Στο δεύτερο, μια **ακριβή** εργασία μπλοκάρει δύο φθηνότερες που μαζί την ξεπερνούν. Το πρόβλημα είναι βαθύ: η σωστή απόφαση για μια εργασία εξαρτάται από όλες τις υπόλοιπες, ενώ ο άπληστος κοιτάζει μόνο μία τη φορά.

<Callout type="key">
Κανένα απλό κριτήριο σειράς δεν αρκεί για τον **σταθμισμένο** χρονοπρογραμματισμό. Χρειαζόμαστε έναν αλγόριθμο που ζυγίζει συστηματικά **όλους** τους συνδυασμούς — χωρίς όμως να τους απαριθμεί έναν προς έναν. Αυτός είναι ο δυναμικός προγραμματισμός.
</Callout>

### Η προετοιμασία: ταξινόμηση και $p(j)$

Πριν στήσουμε την αναδρομή, χρειαζόμαστε δύο προετοιμασίες.

**Πρώτη: ταξινόμηση.** Διατάσσουμε τα αιτήματα κατά **χρόνο λήξης**, ώστε $f_1 \le f_2 \le \dots \le f_n$. Από εδώ και πέρα, «αίτημα $j$» σημαίνει «το $j$-οστό αίτημα που τελειώνει».

**Δεύτερη: η ποσότητα $p(j)$.** Φαντάσου ότι αποφασίζεις να βάλεις το αίτημα $j$ στη λύση σου. Αμέσως αποκλείεις κάθε προηγούμενο αίτημα που επικαλύπτεται μαζί του. Το ερώτημα είναι: ποιο είναι το **τελευταίο** προηγούμενο αίτημα που σου μένει ακόμα διαθέσιμο; Αυτό ακριβώς ονομάζουμε $p(j)$.

<Callout type="key">
$p(j)$ = ο **μεγαλύτερος** δείκτης $i < j$ τέτοιος ώστε το αίτημα $i$ να είναι **συμβατό** με το $j$ (δηλαδή $f_i \le s_j$). Αν κανένα προηγούμενο αίτημα δεν είναι συμβατό, $p(j) = 0$.
</Callout>

Ένα χρήσιμο γεγονός: επειδή τα αιτήματα είναι ταξινομημένα κατά λήξη, τα συμβατά με το $j$ είναι πάντα ένα **αρχικό κομμάτι** $1, \dots, p(j)$ — κι όλα τα ενδιάμεσα $p(j)+1, \dots, j-1$ το επικαλύπτουν. Διάλεξε ένα αίτημα παρακάτω και δες ποιο είναι το $p(j)$ του και ποια αιτήματα αποκλείει:

<PjExplorer />

### Η αναδρομή — μια δυαδική επιλογή

Ορίζουμε $\text{OPT}(j)$ = η τιμή της βέλτιστης λύσης για τα αιτήματα $\{1, 2, \dots, j\}$. Κοιτάμε το αίτημα $j$ και ρωτάμε: **μέσα ή έξω;**

- **Το $j$ είναι ΜΕΣΑ.** Τότε αποκλείονται όλα τα ασύμβατα με το $j$ — τα $\{p(j)+1, \dots, j-1\}$. Ό,τι μένει είναι η βέλτιστη λύση για τα $\{1, \dots, p(j)\}$. Συνεισφορά: $v_j + \text{OPT}(p(j))$.
- **Το $j$ είναι ΕΞΩ.** Τότε η βέλτιστη λύση είναι ακριβώς η βέλτιστη για τα $\{1, \dots, j-1\}$. Συνεισφορά: $\text{OPT}(j-1)$.

Δεν ξέρουμε ποια περίπτωση ισχύει — οπότε παίρνουμε τη **μεγαλύτερη**:

<Callout type="key">
$$
\text{OPT}(j) =
\begin{cases}
0 & j = 0 \\[4pt]
\max\bigl\{\, v_j + \text{OPT}(p(j)),\ \ \text{OPT}(j-1) \,\bigr\} & j \ge 1
\end{cases}
$$
</Callout>

Δες την αναδρομή να γεμίζει τον πίνακα. Σε κάθε βήμα φαίνονται οι δύο υποψήφιες τιμές — «το $j$ μέσα» και «το $j$ έξω» — και ποια κερδίζει:

<WeightedIntervalDP />

### Ο αλγόριθμος

Η απευθείας αναδρομή είναι **εκθετική** — ακριβώς όπως ο αφελής Fibonacci, το πλήθος κλήσεων μεγαλώνει σαν $T(n) = T(n-1) + T(n-2)$. Η θεραπεία είναι η ίδια: **απομνημόνευση**.

<Algorithm
  name="Σταθμισμένος χρονοπρογραμματισμός (DP)"
  idea="Ταξινόμησε κατά λήξη, υπολόγισε τα p(j), και γέμισε τον πίνακα M με την αναδρομή «το j μέσα ή έξω»."
  complexity="O(n log n)"
  io={{
    input: 'n αιτήματα με χρόνους έναρξης/λήξης και βαρύτητες',
    output: 'η μέγιστη συνολική βαρύτητα ενός συνόλου συμβατών αιτημάτων',
  }}
>

**Με λόγια.** Ταξινόμησε τα αιτήματα κατά χρόνο λήξης και υπολόγισε όλα τα $p(j)$. Μετά γέμισε έναν πίνακα $M$ με αύξουσα σειρά: το $M[j]$ είναι το μεγαλύτερο ανάμεσα στο «πάρε το $j$» ($v_j + M[p(j)]$) και στο «παράτα το $j$» ($M[j-1]$). Το $M[n]$ είναι η απάντηση.

<Pseudocode
  code={`σταθμισμένος-χρονοπρογραμματισμός(s, f, v):
  ταξινόμησε τα αιτήματα ώστε f₁ ≤ f₂ ≤ … ≤ fₙ
  υπολόγισε p(1), p(2), …, p(n)
  M[0] ← 0
  για j = 1 έως n:
    M[j] ← max{ vⱼ + M[p(j)],  M[j−1] }
  επίστρεψε M[n]`}
  caption="Bottom-up: ο πίνακας γεμίζει με αύξουσα σειρά, χωρίς αναδρομή."
/>

</Algorithm>

Ισοδύναμα, **top-down με memoization** — η αναδρομή λύνει κάθε υποπρόβλημα, αλλά το αποθηκεύει:

<Pseudocode
  title="Ισοδύναμη εκδοχή — memoization"
  code={`αρχικοποίησε M[1..n] = κενό ;  M[0] = 0

M-ComputeOPT(j):
  αν M[j] = κενό:
    M[j] ← max{ vⱼ + M-ComputeOPT(p(j)),  M-ComputeOPT(j−1) }
  επίστρεψε M[j]`}
/>

<Callout type="key">
**Χρόνος εκτέλεσης: $O(n \log n)$.**
- Ταξινόμηση κατά χρόνο λήξης: $O(n \log n)$.
- Υπολογισμός όλων των $p(j)$: $O(n)$ μετά από μια δεύτερη ταξινόμηση κατά χρόνο έναρξης.
- Γέμισμα του πίνακα: κάθε θέση $M[j]$ υπολογίζεται **μία φορά**, με $O(1)$ δουλειά → $O(n)$ συνολικά.
</Callout>

<ExamProblem
  relatedExerciseId="pt6-th2"
  pattern={
    <>
      <strong>Αν η εκφώνηση γράφει «μη επικαλυπτόμενα αιτήματα με τιμές <InlineMath>{'p_i'}</InlineMath>, μεγιστοποίησε το άθροισμα» σε *οποιαδήποτε* σκηνογραφία</strong> — πλατφόρμα γυμναστηρίου, αίθουσα διδασκαλίας, διαφημιστικό spot, υπολογιστής cloud — είναι ευθεία εφαρμογή του WIS-DP του μαθήματος. Πρώτο βήμα: ταξινόμηση κατά λήξη, <InlineMath>{'p(j)'}</InlineMath>, και η δυαδική «το <InlineMath>{'j'}</InlineMath> μέσα ή έξω». Πολλά τέτοια θέματα ζητούν επιπλέον αντιπαράδειγμα για κάποιον «προφανή» άπληστο — το «κατά τιμή» αποτυγχάνει με το ίδιο σχήμα που δείχνει το <em>GreedyFailsWeighted</em> παραπάνω.
    </>
  }
/>

### Υπολογισμός των $p(j)$ σε $O(n)$

Είπαμε πως όλα τα $p(j)$ βγαίνουν σε $O(n)$. Δεν είναι προφανές: ο απλός τρόπος — για κάθε $j$, ψάξε προς τα πίσω για το τελευταίο συμβατό αίτημα — κάνει $O(n^2)$ δουλειά. Πώς πέφτουμε στο $O(n)$;

Το κόλπο: έχουμε **δύο ταξινομημένες ακολουθίες** — τους χρόνους λήξης και τους χρόνους έναρξης — και τις **συγχωνεύουμε**, ακριβώς όπως το `merge` της συγχωνευτικής ταξινόμησης ([L03](/lectures/L03-divide-and-conquer-i)). Δύο δείκτες, ο $i$ πάνω στις λήξεις και ο $j$ πάνω στις ενάρξεις, σαρώνουν τις ακολουθίες από μία φορά:

- Αν η τρέχουσα λήξη $f_i$ είναι $\le$ από την τρέχουσα έναρξη $s_j$, τότε το αίτημα $i$ προλαβαίνει να τελειώσει — είναι έγκυρος προκάτοχος. Προχωράμε τον $i$.
- Αλλιώς, η λήξη $f_i$ έρχεται πολύ αργά. Άρα ο τελευταίος που πρόλαβε ήταν ο $i-1$: θέτουμε $p(j) = i-1$ και προχωράμε τον $j$.

<Pseudocode
  title="Υπολογισμός των p(j) σε O(n)"
  code={`υπολόγισε-p(λήξεις f, ενάρξεις s):   // και οι δύο ταξινομημένες αύξουσα
  i ← 1 ;  j ← 1
  όσο j ≤ n:
    αν i ≤ n  και  fᵢ ≤ sⱼ:
      i ← i + 1                // το αίτημα i προλαβαίνει — έγκυρος προκάτοχος
    αλλιώς:
      p(j) ← i − 1             // ο τελευταίος που πρόλαβε ήταν ο i−1
      j ← j + 1`}
  caption="Συγχώνευση δύο ταξινομημένων ακολουθιών, όπως το merge — O(n) συνολικά."
/>

Κάθε βήμα προχωρά έναν από τους δύο δείκτες κατά ένα, και κανένας δεν ξεπερνά το $n$ — άρα το πολύ $2n$ βήματα συνολικά. Δες τη σάρωση να τρέχει βήμα-βήμα — στο interactive κάθε έναρξη δείχνει σε ποιο αίτημα ανήκει, ώστε το $p$ να καταγράφεται στο σωστό:

<PjScan />

### Εύρεση της λύσης

Ο αλγόριθμος υπολογίζει τη **μέγιστη τιμή**. Για να βρούμε **ποια αιτήματα** την πετυχαίνουν, κάνουμε ένα πέρασμα προς τα πίσω — σε κάθε $j$ ρωτάμε «ποια από τις δύο περιπτώσεις κέρδισε;»

<Pseudocode
  code={`βρες-λύση(j):
  αν j = 0:  σταμάτα
  αλλιώς αν vⱼ + M[p(j)] > M[j−1]:     // το j ήταν ΜΕΣΑ
    εκτύπωσε j
    βρες-λύση(p(j))
  αλλιώς:                              // το j ήταν ΕΞΩ
    βρες-λύση(j−1)`}
  caption="Το πολύ n κλήσεις → O(n)."
/>

## Η συνταγή του δυναμικού προγραμματισμού

Κάθε πρόβλημα DP — όσα θα δούμε στα [L15](/lectures/L15-dp-ii)–[L17](/lectures/L17-dp-iv) — λύνεται με τα **ίδια 4 βήματα**:

<Callout type="key">
1. **Χαρακτηρισμός της δομής** — όρισε τι είναι ένα «υποπρόβλημα» (εδώ: «τα πρώτα $j$ αιτήματα»).
2. **Αναδρομικός ορισμός** της τιμής της βέλτιστης λύσης — η εξίσωση $\text{OPT}(\cdot)$.
3. **Υπολογισμός** της τιμής — γέμισε τον πίνακα (memoization ή bottom-up).
4. **Κατασκευή** της βέλτιστης λύσης από την αποθηκευμένη πληροφορία — το πέρασμα προς τα πίσω.
</Callout>

<Callout type="intuition">
**Το πιο δύσκολο βήμα είναι το 1ο.** Μόλις βρεις τη σωστή «παραμετροποίηση» των υποπροβλημάτων, η αναδρομή σχεδόν γράφεται μόνη της. Στο [L15](/lectures/L15-dp-ii) θα δούμε πρόβλημα (knapsack) όπου η πρώτη, «προφανής» παραμετροποίηση **δεν δουλεύει** — και χρειάζεται μια δεύτερη μεταβλητή.
</Callout>

<ExamProblem
  relatedExerciseId="pt1-th3"
  pattern={
    <>
      <strong>Αν η εκφώνηση δίνει ακολουθία <InlineMath>{'n'}</InlineMath> καταστάσεων όπου η *τελευταία απόφαση* έχει μικρό σταθερό κατάλογο επιλογών</strong> — ταξί ή πατίνι που καλύπτει <InlineMath>{'4'}</InlineMath> διαδρομές, βήμα ένα ή πέντε, μικρή ή μεγάλη μετακίνηση, … — η συνταγή γράφεται σχεδόν μόνη της: παραμετροποίηση «πρώτα <InlineMath>{'i'}</InlineMath> καταστάσεις», αναδρομή <InlineMath>{'\\min'}</InlineMath>/<InlineMath>{'\\max'}</InlineMath> πάνω στις <InlineMath>{'K'}</InlineMath> επιλογές («πλήρωσα τοπικό κόστος <InlineMath>{'+'}</InlineMath> έλυσα βέλτιστα ό,τι έμεινε πριν»), και πολυπλοκότητα <InlineMath>{'\\Theta(n \\cdot K)'}</InlineMath>.
    </>
  }
/>

<RecallCard
  algorithm="Σταθμισμένος χρονοπρογραμματισμός (DP)"
  keywords={['επικαλυπτόμενα υποπροβλήματα', 'ταξινόμηση κατά λήξη', 'p(j) = τελευταίο συμβατό', 'OPT(j) = max{μέσα, έξω}', 'memoization', 'p(j) σε O(n) με δύο δείκτες']}
  skeleton={[
    'Ταξινόμησε τα αιτήματα κατά χρόνο λήξης· υπολόγισε p(j) για καθένα.',
    'Όρισε OPT(j) = βέλτιστη τιμή για τα πρώτα j αιτήματα.',
    'Αναδρομή: OPT(j) = max{ vⱼ + OPT(p(j)),  OPT(j−1) } — το j μέσα ή έξω.',
    'Γέμισε τον πίνακα M· πέρασμα προς τα πίσω για τα ίδια τα αιτήματα.',
  ]}
  complexity="O(n log n)"
  trap="Ο άπληστος «μικρότερος χρόνος λήξης» (L11) ΑΠΟΤΥΓΧΑΝΕΙ με βαρύτητες — μια φθηνή πρώιμη εργασία μπλοκάρει μια ακριβή. Η αφελής αναδρομή χωρίς memoization είναι εκθετική· ο πίνακας M την κάνει γραμμική."
/>

## Κλείδωσε τη γνώση

Βάλε στη σειρά τα βήματα του DP για τον σταθμισμένο χρονοπρογραμματισμό:

<ReorderDrill
  prompt="Ο αλγόριθμος DP για τον σταθμισμένο χρονοπρογραμματισμό, από την πρώτη ενέργεια στην τελευταία."
  steps={[
    { id: 'w1', text: 'Ταξινόμησε τα αιτήματα κατά αύξοντα χρόνο λήξης.' },
    { id: 'w2', text: 'Υπολόγισε για κάθε j το p(j) — το τελευταίο συμβατό προηγούμενο αίτημα.' },
    { id: 'w3', text: 'Θέσε M[0] = 0.' },
    { id: 'w4', text: 'Για j = 1 έως n, θέσε M[j] = max{ vⱼ + M[p(j)], M[j−1] }.' },
    { id: 'w5', text: 'Η βέλτιστη τιμή είναι το M[n].' },
    { id: 'w6', text: 'Κάνε πέρασμα προς τα πίσω για να βρεις ποια αιτήματα την πετυχαίνουν.' },
  ]}
/>

Και συμπλήρωσε τα κενά:

<ClozeDrill
  prompt="Η ιδέα του δυναμικού προγραμματισμού σε τέσσερις λέξεις-κλειδιά."
  segments={[
    { text: 'Ο δυναμικός προγραμματισμός διασπά ένα πρόβλημα σε ' },
    { blank: 'επικαλυπτόμενα', hint: 'σε αντίθεση με τα ανεξάρτητα του D&C' },
    { text: ' υποπροβλήματα. Με την τεχνική της ' },
    { blank: 'απομνημόνευσης', hint: 'memoization' },
    { text: ' λύνουμε κάθε υποπρόβλημα μία μόνο φορά. Στον σταθμισμένο χρονοπρογραμματισμό, η αναδρομή εκφράζει τη δυαδική επιλογή «το αίτημα j ' },
    { blank: 'μέσα', hint: 'μέσα ή έξω;' },
    { text: ' ή έξω;», και η βέλτιστη τιμή βρίσκεται τελικά στη θέση M[' },
    { blank: 'n', hint: 'ποια θέση;' },
    { text: '].' },
  ]}
/>

## Μοτίβο σκέψης

<ThinkingPattern
  title="Η συνταγή του δυναμικού προγραμματισμού"
  signals={['βρες τη βέλτιστη (μέγιστη / ελάχιστη) …', 'ο άπληστος αποτυγχάνει εδώ', 'κάθε στοιχείο: μέσα ή έξω;']}
>
Όταν ένα πρόβλημα βελτιστοποίησης δεν λύνεται άπληστα, σκέψου DP — με τα ίδια 4 βήματα κάθε φορά:

1. **Δομή / παραμετροποίηση.** Όρισε τι είναι ένα υποπρόβλημα — συνήθως «τα πρώτα $j$ στοιχεία» ή «τα στοιχεία $i..j$». Είναι το δυσκολότερο βήμα· μόλις το βρεις, τα υπόλοιπα γράφονται σχεδόν μόνα τους.
2. **Αναδρομικός ορισμός.** Γράψε το $\text{OPT}(\cdot)$ ως max/min πάνω στις επιλογές της «τελευταίας απόφασης» (εδώ: το $j$ μέσα ή έξω).
3. **Υπολογισμός.** Γέμισε τον πίνακα — memoization (top-down) ή bottom-up. Ίδιο αποτέλεσμα.
4. **Κατασκευή λύσης.** Πέρασμα προς τα πίσω: σε κάθε θέση, ρώτα ποια επιλογή κέρδισε.

**Σήμα ότι θέλεις DP κι όχι divide-and-conquer:** τα υποπροβλήματα **επικαλύπτονται** — το ίδιο εμφανίζεται ξανά και ξανά.
</ThinkingPattern>

<ExamRadar
  items={[
    {
      topic: 'Σχεδίασε αλγόριθμο DP για νέο πρόβλημα — όρισε υποπρόβλημα, αναδρομή, πολυπλοκότητα',
      likelihood: 'high',
      note: 'Το κυρίαρχο θέμα DP — η εκφώνηση ζητάει «σχεδίασε αποδοτικό αλγόριθμο».',
      relatedExerciseIds: ['pt1-th3', 'front-set-8-ask3', 'front-set-8-ask4'],
    },
    {
      topic: 'Σταθμισμένος χρονοπρογραμματισμός — γέμισε τον πίνακα M και βρες τη λύση',
      likelihood: 'high',
      relatedExerciseIds: ['pt6-th2'],
    },
    {
      topic: 'Γιατί ο άπληστος αποτυγχάνει σε πρόβλημα με βάρη — αντιπαράδειγμα',
      likelihood: 'medium',
      relatedExerciseIds: ['pt5-th4', 'pt6-th2'],
    },
    {
      topic: 'Memoization vs bottom-up — και γιατί ο χρόνος πέφτει από εκθετικός σε γραμμικό',
      likelihood: 'medium',
      relatedExerciseIds: ['pt7-th2'],
    },
    {
      topic: 'Διάκριση: πότε DP, πότε divide-and-conquer (επικαλυπτόμενα vs ανεξάρτητα υποπροβλήματα)',
      likelihood: 'medium',
    },
    {
      topic: 'Κάτω φράγμα από το μέγεθος του πίνακα — ποια πολυπλοκότητα ΔΕΝ μπορεί να ισχύει',
      likelihood: 'high',
      note: 'Σταθερή ερώτηση πολλαπλής επιλογής: 1D ⇒ Ω(n), 2D ⇒ Ω(nm). Αποκλείεται ό,τι ασυμπτωτικά μικρότερο.',
      relatedExerciseIds: ['pt1-th1-q7', 'pt1-th1-q8', 'pt2-th1-q6', 'pt2-th1-q7'],
    },
    {
      topic: 'Μέγιστο ανεξάρτητο σύνολο σε μονοπάτι — με ή χωρίς γεωμετρικό περιορισμό',
      likelihood: 'high',
      note: 'OPT(i) = max(OPT(i−1), v_i + OPT(prev(i))). Από κολώνες φωτισμού ως εστιατόρια κατά μήκος δρόμου.',
      relatedExerciseIds: ['pt5-th4', 'front-set-8-ask4'],
    },
    {
      topic: 'Δοκίμασε όλα τα «πρώτα κομμάτια» — τεμαχισμός ράβδου / Θ(n²) DP',
      likelihood: 'medium',
      note: 'C(i) = max_k(V_k + C(i−k)). Όταν το αντικείμενο σπάει σε δύο ανεξάρτητα τμήματα, η αναδρομή δοκιμάζει όλες τις πρώτες κοπές.',
      relatedExerciseIds: ['front-set-8-ask3'],
    },
    {
      topic: 'Εκθετικό κάτω φράγμα αφελούς αναδρομής με k κλάδους',
      likelihood: 'medium',
      note: 'Δέντρο k παιδιών, βάθος ≥ n / max d_i ⇒ Ω(k^(n / max d_i)).',
      relatedExerciseIds: ['pt7-th2'],
    },
  ]}
/>

<Recap>
**Τι κρατάμε από αυτή τη διάλεξη**

1. **Δυναμικός προγραμματισμός** — διάσπαση σε **επικαλυπτόμενα** υποπροβλήματα· λύσε καθένα μία φορά, αποθήκευσέ το.
2. **Fibonacci** — αφελής αναδρομή $O(2^n)$ λόγω επικάλυψης· **memoization** → $O(n)$.
3. **Σταθμισμένος χρονοπρογραμματισμός** — μέγιστη βαρύτητα συμβατών αιτημάτων. Ο άπληστος αποτυγχάνει.
4. $p(j)$ = το τελευταίο συμβατό προηγούμενο αίτημα. Αναδρομή: $\text{OPT}(j) = \max\{v_j + \text{OPT}(p(j)),\ \text{OPT}(j-1)\}$.
5. Χρόνος $O(n \log n)$ — η ταξινόμηση κυριαρχεί· τα $p(j)$ βγαίνουν σε $O(n)$ με σάρωση δύο δεικτών, και η εύρεση της λύσης σε $O(n)$ με πέρασμα προς τα πίσω.
6. **Η συνταγή DP (4 βήματα):** δομή → αναδρομικός ορισμός → υπολογισμός τιμής → κατασκευή λύσης.
</Recap>

<NextUp current="lectures/L14-dp-i" />

<LectureExercises lectureSlug="lectures/L14-dp-i" />
