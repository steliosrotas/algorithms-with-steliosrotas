# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# claude code local config — user-specific files only.
# Project-shared tooling (skills, commands) is checked in.
.claude/*
!.claude/skills/
!.claude/commands/

# typescript
*.tsbuildinfo
next-env.d.ts.local

# sprite preview renders (the script is checked in; outputs are throwaway)
scripts/sprite-preview/tmp/

# NOTE 2026-05-24: the privacy architecture has been DROPPED. Raw past-exam
# and frontistirio source material is now tracked. The bank uses real dates
# («Ιούνιος 2024 · Θ.3»), and the raw PDFs/images live in `private_material/`
# for reference (will be committed when the user uploads them to this work
# tree). See plans/PHASE_E_PLAN.md § E.0 for the bank de-anonymization
# migration, and plans/EXAM_TRANSCRIPTION.md for the old anonymization rule
# (now superseded).
